# 核心问题复盘

日期：2026-07-09

这份文档记录我今天围绕 ARX5 Diffusion Policy / ACT 复现过程中问到的核心问题，以及我现在形成的理解。

## 1. 一个 sample 到底是什么

一个 sample 不是采集时的一行原始数据，而是 dataset 从某条 episode 中切出来的一段训练样本。

在当前 DP 配置里，一个 sample 大致是：

```text
obs:
  camera_0: [2, 3, 240, 320]
  camera_1: [2, 3, 240, 320]
  camera_2: [2, 3, 240, 320]
  robot0_eef_pos: [2, 3]
  robot0_eef_rot_axis_angle: [2, 3]
  robot0_gripper_width: [2, 1]

action:
  [16, 7]
```

含义：

- `2` 是两帧 observation
- `3` 是 RGB 三个颜色通道
- `240, 320` 是图像分辨率
- `16` 是未来 action horizon
- `7` 是每一步 action 的维度

当前 7 维 action 是：

```text
[x, y, z, rx, ry, rz, gripper_width]
```

## 2. batch 是什么

batch 不是一个提示，而是 DataLoader 真实执行的打包规则。

如果：

```text
batch_size = 16
```

那么 DataLoader 会一次取 16 个 sample，并在最前面加一个 batch 维度：

```text
camera_0: [16, 2, 3, 240, 320]
action:   [16, 16, 7]
```

这里 `action: [16, 16, 7]` 里的两个 16 含义不同：

- 第一个 `16` 是 batch size
- 第二个 `16` 是 action horizon
- `7` 是 action 维度

所以不能说“一个 batch 是 7 维”，更准确地说：

> batch 里 action 张量的最后一维是 7 维。

## 3. 张量、key 和 shape 的关系

例如：

```text
camera_2: [2, 3, 240, 320]
```

这里：

- `camera_2` 是数据名字 / key
- `[2, 3, 240, 320]` 是数据形状 / shape
- 真正输入模型的是 `camera_2` 对应的整块 tensor

在代码里是：

```python
obs["camera_2"]
```

取到 tensor 后，可以继续用索引访问其中某个数：

```python
obs["camera_2"][t, c, h, w]
```

如果带 batch：

```python
obs["camera_2"][b, t, c, h, w]
```

## 4. obs 会不会进入网络

会。

obs 是策略网络的条件输入。

当前 obs 包含：

```text
camera_0
camera_1
camera_2
robot0_eef_pos
robot0_eef_rot_axis_angle
robot0_gripper_width
```

它们分别进入不同 encoder：

```text
camera_*  -> ResNet / CNN -> image feature
robot0_*  -> MLP / Linear -> lowdim feature
```

然后融合成 condition，用来指导 UNet 生成 action。

## 5. action、obs 和 UNet 的关系

训练时不是只把 action 输入 UNet，也不是直接预测 loss。

更准确的流程：

```text
obs -> encoder -> obs_condition

clean_action
  -> normalizer
  -> scheduler 加噪
  -> noisy_action

obs_condition + noisy_action + diffusion timestep
  -> UNet
  -> pred_noise

pred_noise vs true_noise
  -> MSE loss
  -> backward
  -> 更新网络参数
```

模型预测的是 `noise`，loss 是预测噪声和真实噪声之间的误差。

## 6. 加噪是加在 obs 上还是 action 上

主要加在 action 上，不是加在 obs 上。

```text
obs = 条件，不加噪
action = 被生成对象，加噪后让 UNet 学去噪
```

obs 告诉模型：

- 胶棒在哪
- 机械臂现在在哪
- 夹爪开合状态是什么

action 是模型要学习生成的未来动作。

## 7. timestep 是什么

Diffusion timestep 是 action 当前的噪声等级，不是机器人真实时间。

例如：

```text
timestep 小：噪声少，只需要轻微修正
timestep 大：噪声多，需要大幅去噪
```

UNet 需要 timestep，是因为它要知道当前 noisy_action 的噪声程度。

通常 timestep 会先变成 embedding，再送入 UNet 的各层。

## 8. UNet 的下采样/上采样和 diffusion 加噪/去噪不是一回事

UNet 内部结构是：

```text
输入
  -> down path
  -> bottleneck
  -> up path
  -> 输出
```

这是网络结构上的编码和解码。

Diffusion 的加噪/去噪是训练和采样流程：

```text
训练：
  clean_action -> 加噪 -> noisy_action -> UNet 预测 noise

部署：
  random_action -> 多次 UNet 去噪 -> final_action
```

所以 UNet 不是一层一层给 action 加噪。

## 9. 需要训练很多个 UNet 吗

不需要。

只训练一个条件 UNet。

同一个 UNet 通过 timestep embedding 学会处理不同噪声等级：

```text
UNet(noisy_action, obs_condition, timestep)
```

部署时也是反复调用同一个 UNet：

```text
random_action
  -> UNet(t=T)
  -> action_T-1
  -> UNet(t=T-1)
  -> ...
  -> final_action
```

## 10. 为什么模型能根据不同状态预测路线

模型不是理解机械臂物理公式，而是从示范数据中学习：

```text
在当前 obs 条件下，人类通常会给出什么未来 action
```

如果 obs 能区分任务阶段，模型就可以学到：

```text
没接触胶棒 -> 下降靠近
对准胶棒 -> 闭合夹爪
夹住胶棒 -> 上抬
```

如果 obs 很像但 action 相反，模型会冲突，表现为抖动或泛化差。

## 11. 为什么不能跨 episode 取 sequence

episode 边界是轨迹断点。

一个 sequence 代表真实连续轨迹：

```text
obs_t -> action_t:t+H
```

如果跨 episode，可能把：

```text
episode 0 的结尾：已经夹住并上抬
episode 1 的开头：回到初始位置并重新接近
```

硬拼在一起。

这会制造物理上不存在的跳变，模型会学到错误轨迹。

正确做法是：

- 可以在不同 episode 之间打乱 sample
- 不能跨 episode 组成连续 sequence

## 12. 为什么不同样本会互相影响参数

所有样本都会更新同一套网络参数。

这不是问题，而是模型学习共同规律的方式。

如果两个样本 obs 不同、action 不同，模型可以学出条件规则：

```text
obs A -> 上抬
obs B -> 下降
```

如果两个样本 obs 很像、action 却相反，就会冲突：

```text
相似 obs -> 一会儿上抬，一会儿下降
```

这种数据会导致模型部署时犹豫、抖动。

## 13. ACT 和 DP 的关系

Diffusion Policy：

```text
obs + noisy_action + timestep -> UNet -> pred_noise
```

ACT：

```text
obs -> Transformer / CVAE -> 未来 action chunk
```

两者都可以使用我当前 ARX5 数据结构：

```text
三路相机 + 7维 qpos/action
```

我已经新建 `arx5_act/`，后续可以用同一套采集数据训练 ACT。

## 14. 当前我对整个训练闭环的理解

当前 ARX5 Diffusion Policy 的训练逻辑可以概括为：

```text
三相机图像 + 机械臂低维状态
  -> 编码成 obs condition

未来 16 步 7维 action
  -> 归一化
  -> 加噪

obs condition + noisy_action + timestep
  -> 条件 UNet
  -> pred_noise

pred_noise 和 true_noise 算 loss
  -> 更新 ResNet / lowdim encoder / UNet
```

部署逻辑可以概括为：

```text
当前 obs
  -> 从随机 action 开始
  -> 多步去噪
  -> 生成未来 action chunk
  -> 机械臂执行前几步
  -> 再观察、再生成、再执行
```

核心结论：

> 模型不是直接记时间，也不是理解物理规则，而是在 obs 条件下学习人类示范的 action 分布。数据越连续、阶段越清楚、示范越一致，部署就越稳。

## 15. 为什么视频短了不能直接照常训练

训练样本要求一一对应：

```text
第 t 步图像 <-> 第 t 步机器人状态 <-> 第 t 步 action
```

如果某个 episode 里低维/action 有 200 步，但视频只能稳定解出 189 帧，那么第 190-200 步就没有可靠图像对应。

这时候有三种处理方式：

```text
1. 报错停止训练
2. 重复最后一帧补齐
3. 裁掉没有图像对应的尾部低维/action
```

我选择第 3 种。

原因是第 2 种会污染训练：

```text
图像不变，但 action 继续变化
```

这会让模型学到不真实的状态-action 对应关系，部署时更容易抖动和乱动。

所以当前项目的做法是：

```text
不改原始 mp4
不改原始 replay_buffer.zarr
只在训练 cache 构建时裁掉尾部无法对齐的 sample
```

## 16. 为什么 DP 训练后可以自动接 ACT

DP 和 ACT 使用的是同一套 ARX5 数据结构：

```text
三路相机图像
robot0_eef_pos
robot0_eef_rot_axis_angle
robot0_gripper_width
action: [x, y, z, rx, ry, rz, gripper_width]
```

区别在于模型训练目标：

```text
DP:
  obs + noisy_action + noise_timestep -> UNet -> pred_noise

ACT:
  obs -> Transformer/CVAE -> future action chunk
```

所以只要数据读取接口统一，就可以在同一份数据上先训练 DP，再训练 ACT。

我写的自动脚本要求：

```text
DP 成功结束 -> 启动 ACT
DP 失败 -> ACT 不启动
```

这样避免 ACT 在错误数据或未完成流程后继续训练。

## 17. 为什么 GPU 利用率可能看起来很低

`nvidia-smi` 的 GPU-Util 是瞬时采样，不是完整训练平均值。

在我的训练中，GPU 可能会等待：

```text
dataloader 取 batch
CPU 解压图像 cache
validation 循环
日志和 checkpoint 写盘
```

所以看到 0.5% 不一定代表没用 GPU。

更可靠的判断方式：

```text
1. config 里 training.device 是否是 cuda:0
2. nvidia-smi 的 Processes 是否有 python
3. Memory-Usage 是否占了几 GB
4. 训练速度是否明显快于 CPU
```

## 18. DP 部署时为什么会有顿感

部署不是简单地“模型预测 action 然后机械臂执行”。中间还有时间调度问题。

当前控制链路是：

```text
相机 + 机器人状态
  -> obs buffer
  -> policy.predict_action
  -> action chunk
  -> 时间戳过滤
  -> ARX5 SDK trajectory
```

如果以下环节处理不好，就会顿：

```text
1. 每次新 chunk 覆盖上一段还没执行完的轨迹
2. chunk 首步和当前实际位姿差太大
3. command latency 太大，机械臂等未来目标点
4. 两段 chunk 边界不连续
5. policy 输出本身高频抖动
```

我目前做的部署层优化是：

```text
用 actual pose 作为 chunk anchor
用 obs timestamp 生成 action 时间表
降低 command latency
after_chunk 模式减少频繁覆盖
chunk 开头做 boundary blend
过滤非递增 target_times
```

这套改动的目标不是改变模型，而是减少执行层引入的机械不连续。

如果后续仍然抖，就要区分是：

```text
模型输出本身抖
还是执行层调度抖
```

判断方法是打印和保存每次 policy chunk：

```text
pose0
pose delta
dt0 / dtN
gripper min/max
是否 clipped
```

如果 policy chunk 本身平滑但实机抖，问题在执行层。
如果 policy chunk 本身跳变，问题在训练数据或模型输出。

## 19. 为什么要把部署层拆成 buffer 和 logger

我现在遇到的问题不是“模型完全不会做任务”，而是机械臂执行时有顿感和跳变。

这种问题不能只靠继续训练解决，因为它可能来自三个不同层面：

```text
1. 模型输出 action chunk 本身不连续
2. chunk 后处理和时间戳调度不连续
3. SDK 执行轨迹时被后续 chunk 覆盖
```

所以我把部署层拆成两块：

```text
trajectory_buffer.py
  负责把 policy chunk 转成连续高频 waypoint

trajectory_logger.py
  负责记录 policy 输出、buffer 插入、实际执行采样
```

这样后续调试时就能看清楚：

```text
policy_chunk 很平滑，但 buffer_sample 抖 -> 执行层问题
policy_chunk 自己就跳 -> 模型或数据问题
policy_chunk 和 buffer_sample 都平滑，但机械臂抖 -> SDK/控制参数/机械臂状态问题
```

这一步的核心价值是把“感觉机械臂很抖”变成可检查的数据，而不是继续盲目调参数。

## 20. 为什么我第一次做 CFG 时效果是反的

我第一次实现 CFG 时，把 `prev_action` 做成了历史动作：

```text
prev_action = 当前 observation 之前已经发生的动作
```

离线验证结果是：

```text
prediction_diff_mean > 0
boundary_improvement < 0
positive_ratio 很低
```

这说明网络确实受到了 `prev_action` 影响，但这个影响方向是错的。

原因是我当时的 `prev_action` 定义和 SAIL 论文不一致。论文里的 action condition 不是历史动作，而是：

```text
上一段 policy chunk 里尚未执行、未来还要继续执行的 tail
```

也就是：

```text
ac = a_He : a_He+Hf
```

这个 condition 的作用不是告诉模型“刚才做过什么”，而是告诉模型：

```text
下一段预测应该和当前正在执行的未来计划保持一致
```

所以我把 dataset 改成：

```text
prev_action_mode = future
prev_action = 当前 action chunk 的前 Hc 步
```

部署时再用上一段 chunk 未执行的未来窗口填入这个 condition。

## 21. CFG 和普通 conditional policy 的区别

普通 conditional policy 是：

```text
obs + prev_action -> network -> action
```

这只是把 `prev_action` 当作普通输入，模型可能会用，也可能会乱用。

SAIL 论文里的 CFG 是在 diffusion denoise 过程中做两次预测：

```text
eps_cond   = U-Net(noisy_action, obs, prev_action)
eps_uncond = U-Net(noisy_action, obs, null_prev_action)
```

然后组合：

```text
eps_guided = eps_uncond + w * (eps_cond - eps_uncond)
```

这意味着：

```text
w = 0:
  完全不使用 prev_action

w = 1:
  使用普通 conditional 结果

w > 1:
  强化 prev_action 对生成轨迹的影响
```

这样做的好处是部署时可以调 `w`，不用重新训练就能比较 consistency 强弱。

## 22. 为什么需要 EAG

CFG 不是越强越好。

如果机械臂已经没有跟上上一段计划，那么上一段 chunk 里剩下的未来 action 可能已经不适合当前真实状态。此时强行 guidance 会导致：

```text
模型被错误 condition 拉偏
机械臂继续追一个已经过时的轨迹
动作变得更抖或更容易失败
```

SAIL 的 EAG 思路是用 tracking error 判断 condition 是否可信：

```text
如果实际 EEF 和 condition action 很接近:
  condition 可信，使用 CFG

如果实际 EEF 和 condition action 差距很大:
  condition 不可信，关闭 CFG
```

当前实现中默认：

```text
pos_threshold = 0.02 m
rot_threshold = 0.05 rad
```

如果超过阈值：

```text
guidance_weight = 0
```

这相当于回退到普通 DP 的 unconditional 预测，避免错误 condition 继续放大。

## 23. 如何判断 CFG 是否真的有用

不能只看训练 loss。CFG 的目标是减少 chunk 间不一致，所以我增加了离线指标：

```text
condition_consistency_improvement(empty-real)
```

它比较两种情况：

```text
empty condition:
  prev_action 全 0

real condition:
  使用真实 future action condition
```

如果指标为正，说明：

```text
真实 condition 让预测 action 更贴近 guidance tail
```

最近一次 mini 验证结果：

```text
prediction_diff_mean=0.672678
condition_consistency_improvement mean=0.061841
positive_ratio=71.88%
gt_first_action_improvement mean=0.212915
positive_ratio=84.38%
```

这个结果说明：

```text
1. condition 确实改变输出
2. 改变方向整体是对的
3. 真实 condition 也让首步 action 更接近 GT
```

所以这版 CFG/EAG 可以进入正式训练验证。

## 24. 当前 CFG 版本和完整 SAIL 的差距

当前项目已经实现：

```text
future action condition
classifier-free guidance denoise blending
tracking-error adaptive guidance
ARX5 continuous deployment 接入
离线 condition consistency 验证
```

但还没有实现完整 SAIL：

```text
1. 没有 adaptive speed modulation
2. 没有训练 policy 预测 critical action / speed factor
3. 没有完整 action scheduling lower-bound 推导
4. 没有系统化 SPARC / CON / LDLJ 指标
5. EAG threshold 还没有针对 ARX5 做网格测试
```

所以在文档或面试里更准确的说法是：

```text
我基于 SAIL 的 CFG/EAG 思路，在 ARX5-DP 上实现了 chunk consistency 原型，
并完成了数据定义、U-Net denoise guidance、部署 EAG 和离线验证链路。
```

而不是说：

```text
我完整复现了 SAIL。
```

## 25. 为什么 CFG 部署会出现回溯

我今天遇到的“回溯”不是单纯 PID 问题，也不是模型完全不会预测，而是 chunk 部署时间线没有对齐。

现象是：

```text
机械臂已经沿着上一段 chunk 往前走了一部分
新 chunk 算出来后，又从上一段 chunk 更早的位置接管
于是机械臂表现为往回抽、闪现、前后抖
```

根本原因是：

```text
policy 推理需要时间
推理期间 executor 仍然在执行旧 chunk
如果新 chunk 的起点没有对齐 executor 当前/未来真实执行位置，就会发生时间错位
```

所以不能简单理解为：

```text
预测 16 步
执行 8 步
然后直接换下一段 16 步
```

更准确地说，部署层必须处理：

```text
1. 推理开始时，executor 已经执行到哪里
2. 推理结束时，executor 又继续走了多少
3. 新 chunk 应该从哪个未来时间点接管
4. 上一段 chunk 的剩余 tail 应该保留多久
```

如果这几个时间点没有统一到同一个 executor 时间轴，chunk 边界就会跳。

## 26. 当前 CFG 部署时间线是什么

当前我把部署逻辑改成以 continuous executor 的未来轨迹为准。

一轮 policy 的逻辑是：

```text
1. executor 正在连续执行上一段 trajectory
2. policy 取当前 obs
3. 从 executor 的未来轨迹里采样 prev_action condition
4. CFG U-Net 生成新的 action chunk
5. drop 掉和 prev_action 重叠的前缀
6. 提交剩余执行段 + tail 给 executor
7. executor 保留旧轨迹到 handoff 时间，再替换未来轨迹
```

日志里如果看到：

```text
source=executor_future
cfg prev valid=6
cfg_drop=6
submit_extra=6
exec_horizon=...
next_delay=...
```

说明当前 condition 来自执行器未来轨迹，而不是静态 last chunk。

这一步很关键，因为 SAIL 里的 action condition 本质上应该代表：

```text
机器人接下来真正会执行的未来计划
```

而不是：

```text
上一段 chunk 数组里某个写死下标之后的数据
```

## 27. 为什么 preview_time 能让动作变平滑

`preview_time` 的作用是把新提交的轨迹整体往未来推。

直观理解：

```text
preview_time = 0:
  新 chunk 很快接管，时间对齐要求很高

preview_time = 0.1:
  executor 多了 0.1s 缓冲，旧轨迹可以继续走，新轨迹晚一点接管
```

所以调大 `preview_time` 往往会让动作更平滑。

但它的代价是：

```text
策略响应变慢
实际执行更滞后
可能降低抓取时对视觉变化的响应能力
```

因此我现在把它当作工程兜底参数，而不是 CFG 的主要解法。

当前更合理的方向是：

```text
1. PREVIEW_TIME 尽量保持 0
2. 用 executor_future condition 对齐时间线
3. 用 replace_future 保留旧轨迹到接管点
4. 用 replace_blend_time 平滑新旧轨迹边界
```

## 28. 为什么 CFG_W=0 不等于原版 DP

我之前用 `CFG_W=0` 测试时发现效果比原版 DP 差很多。

这个现象是合理的，因为：

```text
CFG_W=0 使用的是 CFG 模型里的 unconditional branch
原版 DP 使用的是没有 prev_action 分支的原始模型结构
```

这两个不是同一个东西。

CFG 模型训练时虽然有 `prev_chunk_dropout`，但它的网络结构、normalizer、condition encoder、部署入口都和原版 DP 不完全一样。

所以对比时应该这样理解：

```text
原版 DP baseline:
  scripts/run_dp_pro.sh

CFG-DP ablation:
  scripts/run_dp_cfg_pro.sh + CFG_W=0
```

`CFG_W=0` 只能说明：

```text
这个 CFG 模型在不使用 prev_action condition 时的表现
```

不能说明：

```text
原版 DP 的表现
```

## 29. 为什么当前 CFG 还是没有 ACT 那么丝滑

当前 CFG 版本已经解决了明显回溯，但动作仍然可能不像 ACT 那样丝滑，原因主要有三类。

第一，DP 输出的是离散 waypoint chunk：

```text
action: [16, 7]
```

部署层再把这些点交给 executor 插值执行。  
即使 executor 以 200Hz 执行，本质上仍然是在追一串 20Hz 左右的 waypoint。

第二，CFG 只是在 chunk 边界上增强一致性：

```text
让新 chunk 尽量接上旧 chunk 的 future tail
```

它不会自动把整段路径变成高阶光滑曲线。

第三，ARX5 的真实执行还会受到：

```text
kp/kd
tracking delay
IK 解算
夹爪 torque safety
机械臂本体惯性
```

这些因素影响。

所以当前优化顺序应该是：

```text
1. 先确认没有时间回溯
2. 再调 replace_blend_time
3. 再调 CFG_W 和 EAG threshold
4. 再考虑轨迹后处理或高阶插值
5. 最后才重新训练更大数据或改模型结构
```

## 30. 当前我对 CFG-DP 的最终阶段性判断

当前 CFG-DP 不是失败，但也还没有稳定超过原版 DP。

我现在的判断是：

```text
原版 DP:
  任务成功率和调参稳定性最好，适合作为 baseline

CFG-DP:
  已经实现 SAIL 的核心 action-condition + CFG + EAG 原型
  已经修复部署时间回溯
  适合继续做参数 sweep 和正式训练对比

ACT:
  temporal aggregation 让动作自然，但任务泛化区域和训练目标仍要继续优化
```

如果后续要继续优化 CFG-DP，最值得做的是：

```text
1. 固定同一批测试点，对比原版 DP / CFG_W=0.5 / CFG_W=1.0
2. 记录成功率、是否回溯、是否夹爪 torque hold
3. sweep REPLACE_BLEND_TIME=0.12/0.15/0.18
4. 如果任务成功率下降，再降低 CFG_W 或放宽 EAG
5. 如果动作仍像折线，再考虑轨迹平滑器，而不是继续盲目改训练
```

## 31. 这几天的新结论：为什么要重新做 EEF / Joint 对比

我目前发现同一批数据在不同 action 表达下会出现明显差异：

```text
DP-EEF:
  当前 2026.07.10 glue_motion 版本仍然是最强 baseline。

DP-Joint:
  能更好复刻我的关节运动习惯，有时泛化区域更大，
  但也会放大坏习惯，例如停在胶棒前夹空气、重复低头夹取。

ACT-Joint:
  比 ACT-EEF 更符合 ACT 原版 qpos/action 设定，因此效果更好。

CFG-DP:
  平滑性有提升空间，但当前多组 ckpt 出现固定动作模式，任务性能下降。
```

所以后续不是盲目认定某一种 action 表达更好，而是做同数据对比：

```text
glue_mini -> DP-EEF
glue_mini_joint -> DP-Joint
```

这样可以把变量控制住，判断问题来自：

```text
1. action 表达
2. 数据质量
3. 部署层
4. 模型结构
```

## 32. joint 数据不是重新采的，而是从当前数据转换出来的

当前采集数据本身已经包含：

```text
robot_joint
robot_gripper
robot0_eef_pos
robot0_eef_rot_axis_angle
robot0_gripper_width
action
```

原始 `action` 是 EEF：

```text
[x, y, z, rx, ry, rz, gripper]
```

DP-Joint 需要的 action 是：

```text
[q1, q2, q3, q4, q5, q6, gripper]
```

所以我新增了 `make_joint_dataset.py`，把当前数据复制一份并替换 action：

```text
glue_mini -> glue_mini_joint
```

校验标准是：

```text
max_abs_error(action - [robot_joint, robot_gripper]) = 0
```

这说明 joint 版本是严格可用的。

## 33. “停在胶棒前夹空气”更像闭合时机学习问题

这个问题的关键现象是：

```text
机械臂能朝胶棒靠近，但会提前闭合夹爪。
```

如果日志里 `gripper_minmax` 在末端还没真正包住胶棒时就开始下降，说明模型学到的闭合时机偏早。

这一般不是靠 PID 或 CFG 能根治的，因为模型本身认为此时应该夹。

更合理的数据采集方式是：

```text
快速靠近胶棒附近
最后 2~3 cm 放慢
让胶棒进入夹爪两指之间
再闭合
闭合后保持 0.2~0.5s
再抬起
```

不建议在胶棒前长时间停顿，因为模型可能学到“靠近后发呆”。

## 34. CFG 视觉链路目前没有发现明显未接入，但仍可能策略退化

我检查了 CFG 的：

```text
shape_meta
camera key 顺序
normalizer
obs_encoder trainable params
fixed prev_action + varying obs
fixed obs + varying prev_action
```

结论是：

```text
视觉 encoder 确实参与训练
camera_0/1/2 key 没有明显错位
normalizer 可以加载
prev_action 分支确实生效
```

但模型行为仍可能退化为固定动作，原因可能是：

```text
1. prev_action condition 过强
2. 数据中主导动作模式过强
3. 从零训练 CFG 破坏了原版 DP 的预测能力
4. 在线 prev_action 分布和训练 future prev_action 分布仍不完全一致
```

所以后续 CFG 更适合从最强原版 DP 初始化，再训练新增分支，而不是完全从零训练。

## 35. 相机和 SpaceMouse 属于高频外部状态问题

这几天又遇到了：

```text
spnav.SpnavConnectionException
RuntimeError: Failed to open /dev/video0
RealSense 拔插后消失
```

这些不是模型问题，而是外设状态问题。

SpaceMouse 先查：

```bash
ls /dev/input/by-id | grep -i 3dconnexion
sudo fuser -v /dev/input/by-id/usb-3Dconnexion_SpaceMouse_Wireless_BT-event-joystick
systemctl status spacenavd --no-pager
```

相机先查：

```bash
python - <<'PY'
import cv2
for i in range(0, 20):
    cap = cv2.VideoCapture(i, cv2.CAP_V4L2)
    if cap.isOpened():
        ret, frame = cap.read()
        print(i, ret, None if frame is None else frame.shape)
    cap.release()
PY
```

后续最好把相机绑定从 `/dev/videoN` 改成 serial 或 `/dev/v4l/by-id`。
