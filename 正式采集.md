# ARX5 数据采集操作指南

## 一、环境准备

### 1. 启动 CAN 和加载环境

```bash
cd /media/star/Elyos_PSSD/ARX5/CY_arx5_dp
./start_can1.sh
source ./activate_arx5_env.sh
```

如果 CAN 设备不是默认的 `/dev/ttyACM0`，启动时指定设备名：

```bash
CAN_DEV=/dev/ttyUSB0 ./start_can1.sh
source ./activate_arx5_env.sh
```

---

## 二、采集前检查（夹爪状态确认）

> **注意**：正式采集前必须确保夹爪物理保持打开状态

### 正常启动（夹爪已打开）

```bash
python -m arx5_collector.scripts.prepare_robot_session \
  --model X5 \
  --interface can1 \
  --expected-gripper open
```

### 异常启动（出现负数启动失败时使用）

如果遇到负数启动失败，运行以下命令：

```bash
python -m arx5_collector.scripts.prepare_robot_session \
  --model X5 \
  --interface can1 \
  --expected-gripper open \
  --force-calibrate \
  --negative-startup
```

---

## 三、正式数据采集

> **建议**：正式数据单独存放，使用新的目录名称

当前视频顺序已经按老版本 ARX5 Diffusion Policy 固定为：

```text
0.mp4 = USB wrist camera
1.mp4 = RealSense 0
2.mp4 = RealSense 1
```

训练低维字段也按老版本写入：

```text
robot0_eef_pos
robot0_eef_rot_axis_angle
robot0_gripper_width
```

### 基本采集命令

```bash
python -m arx5_collector.scripts.collect_demo \
  --output data_local/arx5_box_demo_001 \
  --model X5 \
  --interface can1 \
  --usb-device 0
```

当前默认值已经按 SDK SpaceMouse 逻辑调整：

- `--frequency 100`
- `--data-frequency 20`
- `--command-mode traj`
- 启动时自动归位
- 默认不启用 xyz / min-z / 姿态增量限制
- RealSense 默认自动曝光、自动白平衡
- USB 相机默认 `640x480`

如果视频仍然明显卡顿，先用下面这个轻负载命令测试：

```bash
python -m arx5_collector.scripts.collect_demo \
  --output data_local/arx5_box_demo_001 \
  --model X5 \
  --interface can1 \
  --usb-device 0 \
  --camera-pump-fps 20 \
  --preview-fps 5
```

### 50Hz 采集实验

如果需要测试更高频的 lowdim/action，可以把 replay buffer 写入频率设为
`50Hz`。注意相机仍然是 `30fps`，因此会出现多个 action 样本对应同一张图像。

```bash
python -m arx5_collector.scripts.collect_demo \
  --output data_local/arx5_glue_plus_50hz \
  --model X5 \
  --interface can1 \
  --usb-device 0 \
  --frequency 100 \
  --data-frequency 50 \
  --camera-pump-fps 30 \
  --preview-fps 5
```

采完后检查 `dt_mean` 是否接近 `0.020s`：

```bash
python -m arx5_collector.scripts.analyze_recordings \
  data_local/arx5_glue_plus_50hz
```

### 调试模式（可选）

如需查看夹爪和SpaceMouse的实时数值，可添加调试参数：

```bash
# 在以上命令末尾添加
--debug-teleop
```

> **注意**：正式录数据时建议**不加** `--debug-teleop`，终端输出会更清晰。

---

## 四、相机参数

### RealSense

RealSense 现在默认全部自动适应，不传 `--realsense-config` 时会自动设置：

- auto exposure
- auto white balance

所以正式采集一般不用额外指定 RealSense 参数。

### USB 相机调参

需要重新调 USB 相机时运行：

```bash
python scripts/camera/usb/tune_usb_camera.py \
  --device 0 \
  --width 640 \
  --height 480 \
  --fps 30
```

按键：

| 按键 | 功能 |
|------|------|
| `a` | 自动曝光 + 自动白平衡 |
| `m` | 切到手动曝光/增益/白平衡 |
| `[` / `]` | 减小 / 增大曝光 |
| `-` / `=` | 减小 / 增大增益 |
| `,` / `.` | 减小 / 增大白平衡 |
| `1` / `2` | 减小 / 增大亮度 |
| `3` / `4` | 减小 / 增大对比度 |
| `5` / `6` | 减小 / 增大饱和度 |
| `q` | 保存参数并退出 |

退出后会同时保存：

```text
scripts/camera/usb/HHMM.yaml
scripts/camera/usb/current.yaml
```

采集时使用当前 USB 参数：

```bash
python -m arx5_collector.scripts.collect_demo \
  --output data_local/arx5_box_demo_001 \
  --model X5 \
  --interface can1 \
  --usb-device 0 \
  --usb-config scripts/camera/usb/current.yaml
```

---

## 五、采集过程中的快捷键控制

| 按键 | 功能 |
|------|------|
| `c` | 开始录制当前episode |
| `s` | 保存当前episode |
| `d` | 丢弃当前episode并归位 |
| `r` | 不录制时归位 |
| `q` | 保存当前episode，归位并退出程序 |
| `Backspace` | 删除上一个已保存的episode |
| `Space` | 标记stage（阶段标记） |

---

## 六、采集后的验证步骤

### 1. 采集测试

先录制 **1个10-20秒的正式episode**，然后正常退出。

### 2. 验证数据完整性

```bash
find data_local/arx5_box_demo_001 -maxdepth 4 -type f | sort | head -80
```

更推荐直接跑数据审计：

```bash
python -m arx5_collector.scripts.analyze_recordings \
  data_local/arx5_box_demo_001
```

训练前再检查一次实际训练 dataset。这个检查会按训练配置把 100Hz
旧数据重采样到 20Hz；新采数据默认已经按 20Hz 写入，所以只会做轻量对齐和缓存：

```bash
python -m arx5_collector.scripts.inspect_training_dataset \
  --dataset-path data_local/arx5_box_demo_001
```

### 3. 检查要点

- ✅ `replay_buffer.zarr` 是否存在
- ✅ 三路摄像头视频文件是否齐全（`videos/` 目录下）
- ✅ 视频顺序是否为 `0=USB, 1/2=RealSense`
- ✅ `robot0_*` 低维字段是否存在
- ✅ 训练 dataset 是否已经重采样到接近旧版 DP 的 episode 长度
- ✅ `inspect_training_dataset` 是否显示 `Training readiness: PASS`
- ✅ 如果静止比例过高，下一轮采集应减少开始/结束等待时间
- ✅ 文件大小和时间戳是否合理

---

## 七、参数说明

| 参数 | 说明 | 默认/推荐值 |
|------|------|-------------|
| `--output` | 数据输出目录 | 自定 |
| `--model` | ARX5型号 | X5 / L5 |
| `--interface` | 通信接口 | can1 |
| `--usb-device` | USB摄像头设备号 | 0 |
| `--realsense-config` | RealSense参数文件 | 默认自动，不需要传 |
| `--usb-config` | USB相机参数文件 | 可用 `scripts/camera/usb/current.yaml` |
| `--frequency` | SpaceMouse 和机械臂控制频率（Hz） | 100 |
| `--data-frequency` | replay buffer 低维数据/action 写入频率（Hz） | 20，50Hz 实验时手动指定 |
| `--reset-to-home-start` | 采集前自动归位 | 启用 |
| `--camera-pump-fps` | 相机后台线程取帧/写入频率 | 30 |
| `--preview-fps` | 预览窗口刷新频率 | 15 |
| `--pos-speed` | SpaceMouse位置速度 | 0.4 |
| `--rot-speed` | SpaceMouse旋转速度 | 0.75 |
| `--gripper-speed` | 夹爪速度 | 0.08 |
| `--gripper-margin` | 夹爪边界安全余量 | 0.0 |
| `--min-z` | 最小Z轴高度限制 | 默认关闭 |
| `--max-pos-delta` | 最大位置增量限制 | 默认关闭 |
| `--max-x-delta` | X方向最大增量限制 | 默认关闭 |
| `--max-y-delta` | Y方向最大增量限制 | 默认关闭 |
| `--max-z-delta` | Z方向最大增量限制 | 默认关闭 |
| `--max-rot-delta` | 最大旋转增量限制 | 默认关闭 |
| `--command-mode` | 控制模式 | traj |
| `--disable-usb-recording` | 只录RealSense，用于排查USB编码瓶颈 | 默认关闭 |

---

## 八、工作流程总结

```
1. 激活环境 → 2. 检查夹爪 → 3. 开始采集 → 4. 录制episode → 5. 验证数据 → 6. 构建/复用训练缓存
```

### 完整操作序列

```bash
# Step 1: 激活环境
cd /media/star/Elyos_PSSD/ARX5/CY_arx5_dp
./start_can1.sh
source ./activate_arx5_env.sh

# Step 2: 检查夹爪（正常情况）
python -m arx5_collector.scripts.prepare_robot_session \
  --model X5 --interface can1 --expected-gripper open

# Step 3: 开始采集
python -m arx5_collector.scripts.collect_demo \
  --output data_local/arx5_box_demo_001 \
  --model X5 --interface can1 --usb-device 0 \
  --camera-pump-fps 30 --preview-fps 15

# Step 4: 采集验证
find data_local/arx5_box_demo_001 -maxdepth 4 -type f | sort | head -80
```

---

## 九、常见问题

### Q1: 夹爪启动失败怎么办？
使用带 `--force-calibrate --negative-startup` 的修复命令。

### Q2: 采集过程中想放弃当前episode？
按 `d` 键即可丢弃并归位。

### Q3: 如何确保数据质量？
建议采集后立即运行验证命令，检查zarr文件和视频文件是否完整对应。

---

**下一步**：完成首次测试采集后，将验证命令的输出结果发送给开发人员确认数据格式是否正确。

## 十、启动脚本(每次新开始都这么搞)

```bash
cd /media/star/Elyos_PSSD/ARX5/CY_arx5_dp
./start_can1.sh #这个是启动can1的
source ./activate_arx5_env.sh #这个是在新的窗口添加环境变量的
```
