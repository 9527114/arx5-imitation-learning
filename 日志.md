# 2026-07-07 调试日志

## 1. 环境搭建

### 问题

安装 `arx5-dp` 环境时，`mujoco` 的 pip wheel 编译失败：

```text
ERROR: Failed building wheel for mujoco
ERROR: Could not build wheels for mujoco
```

### 处理

当前目标是先跑真实 ARX5 数据采集，暂时不依赖仿真环境，所以先绕过 Mujoco 仿真相关问题，优先保证：

- `diffusion_policy-main` 可以导入
- `arx5-sdk-main/python` 可以导入
- RealSense / USB 相机可以打开
- SpaceMouse 可以读取
- ARX5 CAN 通信可以启动

后续如果要补仿真训练或仿真验证，再单独处理 Mujoco 编译依赖。

---

## 2. ARX5 SDK 动态库加载问题

### 问题

运行 SDK 示例时连续遇到动态库缺失：

```text
ImportError: libhardware.so: cannot open shared object file
ImportError: libspdlog.so.1.17: cannot open shared object file
ImportError: libsoem.so: cannot open shared object file
undefined symbol: EcatError
```

### 原因

Python 能找到 `arx5_interface` 不代表底层 `.so` 依赖都能找到。ARX5 SDK 需要同时让 Python 搜索路径和动态库搜索路径指向本地 SDK。

### 解决方案

每次开新终端后先执行：

```bash
conda activate arx5-dp
export LD_LIBRARY_PATH=$CONDA_PREFIX/lib:/media/star/Elyos_PSSD/ARX5/CY_arx5_dp/arx5-sdk-main/lib/x86_64:$LD_LIBRARY_PATH
export PYTHONPATH=/media/star/Elyos_PSSD/ARX5/CY_arx5_dp/arx5-sdk-main/python:$PYTHONPATH
```

当前 `arx5_collector` 内部也通过 `sdk_path.py` 自动补了 SDK Python 路径，降低脚本运行时找不到 `arx5_local_config.py` 的概率。

---

## 3. 夹爪启动负数与方向反问题

### 问题

多次启动时出现：

```text
Gripper position error: got -0.022 but should be in 0~0.088 (m)
Gripper position error: got -0.087 but should be in 0~0.088 (m)
```

尝试把 `gripper_open_readout` 改成 `-3.4` 后，又出现夹爪方向反和电流过冲：

```text
Over current detected once on gripper
Over current detected, robot is set to damping
```

### 原因

ARX5 夹爪的米制开合量不是传感器直接给的，而是 SDK 用电机 raw angle 换算出来的：

```text
gripper_pos = raw_angle / gripper_open_readout * gripper_width
```

所以只要下面任意一项不匹配，SDK 就会算出负数、方向反，或者把物理闭合误认为打开：

- 当前电机 raw zero
- `gripper_open_readout`
- 夹爪实际物理状态

本机这只夹爪不能用 `-3.4`，正确的 fully-open readout 是正数，今天重新测到：

```text
Fully-open joint position readout: 5.04635
```

### 解决方案

把本机参数集中到：

```text
arx5-sdk-main/python/arx5_local_config.py
```

当前关键值：

```python
GRIPPER_OPEN_READOUT = 5.04635
GRIPPER_WIDTH = 0.088
GRIPPER_TORQUE_MAX = 2.0
GRIPPER_KP = 5.2
GRIPPER_KD = 0.8
```

正式采集前先跑半自动检查脚本：

```bash
cd /media/star/Elyos_PSSD/ARX5/CY_arx5_dp/diffusion_policy-main
python -m arx5_collector.scripts.prepare_robot_session \
  --model X5 \
  --interface can1 \
  --expected-gripper open
```

如果正常启动失败并报负数，再运行：

```bash
python -m arx5_collector.scripts.prepare_robot_session \
  --model X5 \
  --interface can1 \
  --expected-gripper open \
  --force-calibrate \
  --negative-startup
```

按提示操作：

1. 提示 fully close 时，手动完全闭合夹爪再回车
2. 提示 fully open 时，手动完全打开夹爪再回车
3. 如果打印出的 fully-open readout 明显变了，更新 `arx5_local_config.py`

---

## 4. SpaceMouse 控制与 IK 失败

### 问题

早期 SpaceMouse 有两个现象：

- SpaceMouse 数值在变，但是机械臂不动
- 终端反复出现：

```text
Inverse kinematics failed: E_EXCEED_JOINT_LIMIT (-9)
```

### 原因

当时目标位姿离当前工作空间边界太近，尤其是 z 方向曾经被压到较低位置，导致 IK 求解经常超关节限制。同时脚本里额外加过 xyz / min-z 限制，和 SDK 原始 SpaceMouse 行为不一致，造成手感和可达空间异常。

### 解决方案

采集脚本已经改成尽量贴近 SDK 示例的控制逻辑：

- 默认启动后先 `reset_to_home`
- 默认 `command-mode=traj`
- 默认控制频率 `100Hz`
- 默认不启用 xyz / min-z / 姿态增量限制
- SpaceMouse 坐标方向在 `arx5_local_config.py` 里统一改

当前 SpaceMouse 方向配置：

```python
SPACEMOUSE_TRANSLATION_SIGN = [-1, -1, 1]
SPACEMOUSE_ROTATION_SIGN = [1, 1, 1]
```

如果后续只需要改方向，优先改 `arx5_local_config.py`，不要分散改多个脚本。

---

## 5. 夹爪控制过慢或卡住

### 问题

SpaceMouse 左右键能够打印 intent 和目标值，例如：

```text
buttons left=True right=False intent=close gripper_target=0.0203
buttons left=False right=True intent=open gripper_target=0.0209
```

但实际夹爪不动、很慢，或者张开到某个值附近卡住。

### 原因

主要和以下配置相关：

- 夹爪零点不正确时，SDK 的米制位置会反
- `gripper_kp/gripper_kd` 不合适时，夹爪响应弱
- `gripper_margin` 会限制最大打开或闭合边界

### 解决方案

当前固定了：

```python
GRIPPER_KP = 5.2
GRIPPER_KD = 0.8
```

采集脚本默认：

```text
--gripper-speed 0.08
--gripper-margin 0.0
```

其中 `gripper-margin` 是夹爪边界安全余量。比如 width 是 `0.088`，margin 是 `0.006` 时，目标会被限制在：

```text
0.006 ~ 0.082
```

如果想让夹爪尽量全开全合，保持 `--gripper-margin 0.0`。

---

## 6. RealSense 与 USB 相机

### 问题

RealSense 测试最开始找不到原作者机器上的硬编码配置：

```text
FileNotFoundError: /home/cchi/dev/diffusion_policy/...
```

USB 相机自动曝光也出现过忽亮忽暗、一片白、录制后打不开等问题。

### 解决方案

已补相机参数读取和保存接口：

- RealSense 参数放在 `diffusion_policy-main/scripts/camera/realsense/`
- USB 参数放在 `diffusion_policy-main/scripts/camera/usb/`
- 统一接口在 `diffusion_policy-main/diffusion_policy/real_world/camera_config_util.py`

最终 USB 相机先保持自动模式，不再强行手动写曝光和白平衡。

---

## 7. 数据采集 episode 保存逻辑

### 问题

出现过按 `s` 或 `c` 后没有数据，或只生成两个 RealSense 视频、没有 replay buffer 数据：

```text
Window save requested. no active episode to save.
```

### 原因

之前按键语义不清楚，`c` 曾经既承担开始又承担保存，容易误以为已经开始录制。实际上没有 active episode 时，`s` 只会提示没有可保存数据。

### 解决方案

当前采集按键统一为：

| 按键 | 功能 |
|------|------|
| `c` | 开始当前 episode |
| `s` | 保存当前 episode |
| `d` | 丢弃当前 episode，并归位 |
| `r` | 不录制时归位 |
| `q` | 保存当前 episode，归位并退出 |
| `Backspace` | 删除上一个已保存 episode |
| `Space` | stage 标记 |

`DPEpisodeWriter` 也增加了 sample count 检查：如果 episode 里 0 个样本，会直接丢弃，避免生成空数据。

---

## 8. 录制时机械臂卡顿与视频失真

### 问题

开启录制后出现：

- SpaceMouse 控制机械臂一卡一卡
- 相机预览和录制视频有强烈失真感
- 降低预览帧率后，机械臂控制改善，但视频仍然卡

### 原因

早期主循环同时做了：

- 100Hz 机械臂控制
- RealSense 取帧
- USB 取帧
- 画面 resize 和 preview
- USB 视频编码
- replay buffer 写入

这些操作放在同一个主循环里会阻塞实时控制和相机写入。只降低预览帧率不够，因为取帧和视频写入仍然在主循环里。

### 解决方案

当前已做拆分：

- 主线程只负责机械臂控制、SpaceMouse、低维状态/action 写入
- 相机线程负责 RealSense/USB 取帧、USB 写入、preview 生成
- 新增参数：
  - `--camera-pump-fps`
  - `--preview-fps`
  - `--disable-usb-recording`
- USB 默认降到 `640x480`
- USB H264 编码使用 `crf=23`，并开启少量线程

如果视频仍然卡，优先用下面命令定位是不是 USB 编码瓶颈：

```bash
python -m arx5_collector.scripts.collect_demo \
  --output data_local/arx5_box_demo_rs_only_test \
  --model X5 \
  --interface can1 \
  --usb-device 0 \
  --disable-usb-recording \
  --preview-fps 5
```

如果禁用 USB 录制后 RealSense 视频明显正常，说明瓶颈主要在 USB 取帧/编码；下一步应该继续降低 USB 分辨率、降低 `--camera-pump-fps`，或者把 USB 编码格式改得更轻。

---

## 9. 当前推荐正式采集命令

```bash
cd /media/star/Elyos_PSSD/ARX5/CY_arx5_dp/diffusion_policy-main
export LD_LIBRARY_PATH=$CONDA_PREFIX/lib:/media/star/Elyos_PSSD/ARX5/CY_arx5_dp/arx5-sdk-main/lib/x86_64:$LD_LIBRARY_PATH

python -m arx5_collector.scripts.collect_demo \
  --output data_local/arx5_box_demo_001 \
  --model X5 \
  --interface can1 \
  --usb-device 0
```

如果视频仍有卡顿，先降低相机线程负载：

```bash
python -m arx5_collector.scripts.collect_demo \
  --output data_local/arx5_box_demo_001 \
  --model X5 \
  --interface can1 \
  --usb-device 0 \
  --camera-pump-fps 20 \
  --preview-fps 5
```

---

## 10. 下一步

下一步先采一条 10-20 秒正式 episode，然后检查：

```bash
find data_local/arx5_box_demo_001 -maxdepth 4 -type f | sort | head -80
```

确认 `replay_buffer.zarr` 和三路视频都存在后，再开始批量采集。

---

# 2026-07-09 项目整理与老版本 DP 方法对齐

## 11. 当前版本和老版本的核心差异

### 现象

当前项目已经可以完成：

- ARX5 SpaceMouse 遥操作
- 三相机录制
- replay buffer 数据保存
- Diffusion Policy 训练
- ckpt 在线加载和部署

但部署效果不稳定，尤其是：

- 模型容易受衣服颜色、环境光照影响
- 旧 ckpt 用当前部署脚本时会出现奇怪姿态
- 当前数据结构和 `try_/arx5_diffusion` 老版本不完全一致

### 排查结果

对比 `/media/star/Elyos_PSSD/ARX5/try_/arx5_diffusion` 后发现主要差异不是网络本身，而是工程接口：

| 项目 | 老版本 | 当前早期版本 |
|------|--------|--------------|
| camera_0 | USB wrist camera | RealSense 0 |
| camera_1 | RealSense 0 | RealSense 1 |
| camera_2 | RealSense 1 | USB |
| 低维观测 | `robot0_eef_pos` / `robot0_eef_rot_axis_angle` / `robot0_gripper_width` | `robot_eef_pose` / `robot_gripper` |
| action | `[target_pose_6d, gripper]` | 相同，但执行方式更简单 |
| 在线执行 | action chunk + timestamp 调度 | 单步选取 action 执行 |

### 结论

当前项目要继续优化，应该先回到老版本的数据语义和相机语义，再逐步恢复老版本的 action chunk 执行逻辑。

---

## 12. 数据结构回到老版本 DP schema

### 改动

`DPEpisodeWriter` 现在写入老版本字段：

```text
robot0_eef_pos              shape: (T, 3)
robot0_eef_rot_axis_angle   shape: (T, 3)
robot0_gripper_width        shape: (T, 1)
action                      shape: (T, 7)
```

同时保留当前项目的兼容字段：

```text
robot_eef_pose
robot_gripper
robot_eef_pose_vel
robot_joint
robot_joint_vel
target_gripper
gripper_torque
stage
timestamp
```

### 作用

这样新采的数据可以直接按老版本 DP 的 `shape_meta` 训练，同时当前调试脚本仍然能读取扩展状态。

### 已迁移数据

对当前默认数据集执行了迁移：

```bash
python -m arx5_collector.scripts.migrate_to_old_dp_schema \
  data_local/arx5_glue_test
```

结果：

```text
robot0_eef_pos: (36281, 3)
robot0_eef_rot_axis_angle: (36281, 3)
robot0_gripper_width: (36281, 1)
```

---

## 13. 三相机顺序回到老版本

### 旧问题

早期当前项目录制顺序是：

```text
0.mp4 = RealSense 0
1.mp4 = RealSense 1
2.mp4 = USB
```

但老版本 ARX5 DP 约定是：

```text
0.mp4 = USB wrist camera
1.mp4 = RealSense 0
2.mp4 = RealSense 1
```

如果相机顺序不一致，模型会把不同视角当成同一个输入 key。比如旧模型训练时 `camera_0` 是腕部 USB，部署时却给了 RealSense，这会导致策略行为完全错位。

### 解决方案

`ThreeCameraRecorder` 已改成老版本顺序：

```text
videos/<episode>/0.mp4 = USB
videos/<episode>/1.mp4 = RealSense 0
videos/<episode>/2.mp4 = RealSense 1
```

采集预览顺序也同步改成：

```text
camera_0=usb | camera_1=realsense_0 | camera_2=realsense_1
```

### 旧数据处理

如果某个旧数据集还是当前早期顺序，可以先 dry-run：

```bash
python -m arx5_collector.scripts.reorder_videos_to_old_dp_schema \
  data_local/arx5_glue_test
```

确认后再执行：

```bash
python -m arx5_collector.scripts.reorder_videos_to_old_dp_schema \
  data_local/arx5_glue_test \
  --apply
```

---

## 14. 训练配置回到老版本观测 key

### 改动

`diffusion_policy-main/diffusion_policy/config/task/arx5_image.yaml` 已改为：

```yaml
obs:
  camera_0:
    shape: [3, 240, 320]
    type: rgb
  camera_1:
    shape: [3, 240, 320]
    type: rgb
  camera_2:
    shape: [3, 240, 320]
    type: rgb
  robot0_eef_pos:
    shape: [3]
    type: low_dim
  robot0_eef_rot_axis_angle:
    shape: [3]
    type: low_dim
  robot0_gripper_width:
    shape: [1]
    type: low_dim
action:
  shape: [7]
```

同时把 `val_ratio` 从 `0.00` 改成 `0.10`，训练时留出一部分 episode 做 validation，避免只看训练 loss。

### 面试可讲点

这里的关键不是“改名字”，而是让采集、训练、推理三条链路使用同一套 observation contract。机器人学习里很多部署失败不是模型结构问题，而是数据语义在工程链路里发生了错位。

---

## 15. 数据审计脚本增强

### 问题

之前采完数据只能看有没有文件，不能快速判断数据质量。比如：

- episode 是否过长
- 静止 action 是否太多
- action 范围是否异常
- 三路视频是否完整
- 视频 FPS 是否稳定

### 解决方案

增强了：

```bash
python -m arx5_collector.scripts.analyze_recordings \
  data_local/arx5_glue_test
```

现在会输出：

```text
episode count
total steps
episode length summary
global static action ratio
action min/max/mean
robot0_* min/max/mean
camera frame count / duration / fps / bitrate
camera static frame ratio
```

当前 `data_local/arx5_glue_test` 审计结果：

```text
episodes = 28
steps = 36281
episode length min/mean/max = 1008 / 1295.8 / 1643
global static action ratio = 32.6%
```

### 结论

这组数据能跑通训练，但 episode 偏长，静止比例偏高。后续正式数据建议每条 episode 控制在：

```text
200 - 500 frames
```

减少开始前等待、任务后停顿和无效移动。

---

## 16. 当前训练和部署状态

### 已完成

当前已经完成一个 ARX5 版 DP 的最小闭环：

```text
ARX5 SDK
  -> SpaceMouse teleop
  -> 三相机采集
  -> DP replay_buffer.zarr + videos
  -> Arx5ImageDataset
  -> train.py 训练
  -> ckpt loader
  -> 在线策略推理/执行
```

训练结束示例：

```text
epoch = 199
global_step = 451199
train_loss = 0.00036
train_action_mse_error = 0.0
```

### 注意

loss 很低不一定代表真实部署好。当前任务数据量较小、场景颜色明显，模型可能学到背景或颜色偏差。例如黄色胶棒任务中，模型曾经被橙色衣服吸引，说明视觉泛化不足。

后续优化应优先从数据分布入手：

- 固定曝光和白平衡
- 控制窗边强光
- 增加不同光照下的演示
- 加入衣服颜色、干扰物变化
- 让胶棒位置和背景更加多样

---

## 17. 后续技术债和优化方向

### 1. 训练数据处理回到旧版 DP 分布

旧版 `try_/arx5_diffusion` 的数据不是简单地把机器人状态高速写进去，而是通过 `Arx5RealEnv.get_obs()` 做了：

```text
相机时间戳对齐
机器人状态插值到观测时间点
20Hz 左右的策略频率
```

当前采集链路虽然控制是 100Hz，但如果训练也直接吃 100Hz 数据，会导致：

- 每条 episode 过长
- 大量相邻样本几乎重复
- 相机帧被重复使用
- 模型更容易学到“保持不动”或静止段

因此当前 `Arx5ImageDataset` 已增加训练前处理：

```text
target_frequency=20.0
delta_action=False
trim_static_start_end=False
```

默认训练时会把 replay buffer 按 `timestamp` 重采样到 20Hz，再构建训练序列。这样数据分布更接近旧版 DP。

随后采集脚本进一步优化为“控制频率”和“数据写入频率”分离：

```text
--frequency 100       # SpaceMouse 和机械臂控制频率
--data-frequency 20   # replay_buffer.zarr 写入频率
```

这样新采数据在采集时就已经接近旧版 20Hz 训练分布，不需要先写 100Hz 再离线压缩；同时遥操作手感仍保持 100Hz。

新增检查命令：

```bash
python -m arx5_collector.scripts.inspect_training_dataset \
  --dataset-path data_local/arx5_glue_test
```

可选静止段裁剪：

```bash
python -m arx5_collector.scripts.inspect_training_dataset \
  --dataset-path data_local/arx5_glue_test \
  --trim-static
```

如果裁剪效果合理，训练时可以打开：

```bash
task.dataset.trim_static_start_end=True
```

### 2. 恢复老版本 action chunk 执行

老版本 `eval_arx5.py` 的核心优势是：

```text
get aligned observation
policy predict action chunk
给每个 action 生成未来 timestamp
exec_actions(actions, timestamps)
```

当前已经把 `arx5_ckpt_loader/run_arx5_policy.py` 改成默认 action chunk + timestamp 调度：

```text
policy.predict_action()
取 cfg.n_action_steps 个未来 action
按 frequency 生成未来 target timestamps
丢弃已经过期的 action
通过 Arx5Robot.schedule_waypoints() 一次提交轨迹 chunk
```

如果需要回退到早期单步执行模式，可以在部署命令里添加：

```bash
--single-action
```

同时修复了一个在线相机映射问题：`ThreeCameraRecorder` 实时推理时现在也按老版本顺序映射：

```text
camera_0 = USB
camera_1 = RealSense 0
camera_2 = RealSense 1
```

### 2. 做数据裁剪或采集规范

当前数据的静止 action 比例约 32.6%，episode 平均 1295 steps，偏长。更好的方向是：

- 采集时只保留任务执行阶段
- 或写脚本按 action 变化自动裁剪开头/结尾静止段
- 每次训练前先跑 `analyze_recordings`

### 3. 统一文档入口

新增了：

```text
diffusion_policy-main/ARX5_WORKFLOW.md
```

这里记录当前推荐的：

- 数据 schema
- 采集命令
- 迁移命令
- 审计命令
- 训练命令
- 部署命令

以后优先维护这份文档，避免命令散落在聊天记录里。

---

## 18. 面试表达版本

可以这样概括这个项目：

> 我把原始 Diffusion Policy 的真实机器人流程从 UR5 迁移到了 ARX5。工作重点不是只跑通训练，而是把真实硬件的控制、三相机同步采集、SpaceMouse 遥操作、replay buffer 数据格式、训练配置和在线部署连成闭环。调试中发现部署效果很大程度取决于数据语义一致性，所以我把当前项目的数据 schema 和相机顺序重新对齐到已有可工作的 ARX5 旧版本，并补了数据审计、迁移和视频重排工具，方便后续稳定采集和训练。

核心工程点：

- 解决 ARX5 SDK 动态库、CAN、夹爪零点和 SpaceMouse 控制问题
- 将 ARX5 状态映射为 DP 训练需要的低维 observation 和 7D action
- 实现两台 RealSense + 一台 USB 的三相机采集
- 将相机录制和机器人控制拆线程，解决录制时控制卡顿和视频失真
- 构建 ARX5 专用 dataset、训练配置和 ckpt 在线加载脚本
- 恢复接近原版 DP 的 action chunk timestamp 调度
- 增加数据质量审计和 schema 迁移工具

下一步可以继续讲：

- 实机验证 chunk 调度的延迟参数和控制平滑性
- 增加数据多样性，缓解颜色和光照过拟合
- 将同一套数据接口扩展到 ACT / transformer policy

---

## 19. 数据处理继续优化

本轮重点优化了训练数据对齐和转换速度。

### 1. 采集端时间对齐

之前按 `c` 后，视频会按 `start_time = now + 0.5s` 延迟启动，但
`replay_buffer.zarr` 的低维/action 会立刻写入，导致每个 episode 前面
可能多出约 0.5 秒没有视频对应的数据。

现在低维/action 也会等到同一个 `start_time` 后才开始写入。这样：

- `timestamp`
- `action`
- `robot0_*`
- 三路视频

会从同一个 episode 起点开始对齐。

### 2. 训练转换速度

旧的 100Hz 数据如果直接转训练 cache，会先解码大量重复视频帧，再重采样。
现在 dataset 会先根据 `timestamp` 预选 20Hz 样本，再只解码这些训练会用到
的帧。

在 `data_local/arx5_glue_v2` 上验证结果：

```text
ARX5 preselected training samples: 152 -> 151 steps target_frequency=20Hz
Loading image data: 453 frames
Training readiness: PASS
```

### 3. 训练前检查

`inspect_training_dataset` 现在会输出训练可用性：

```text
Training readiness:
  PASS
  warning: only 1 episode(s); use this for pipeline tests, not final training
  warning: high static action ratio: 42.7%
```

结论：`arx5_glue_v2` 可以验证训练管线，但正式训练需要更多 episode，并尽量降低
静止 action 比例。

---

## 20. 2026-07-09 数据、训练、部署与 ACT 扩展

今天我把 ARX5 版 Diffusion Policy 从“能跑通”继续推进到“更像我自己的项目闭环”：
采集数据、训练数据处理、50Hz 高频实验、部署稳定性保护、以及 ACT 扩展入口都已经整理到当前项目中。

### 1. 我完成了 50Hz 数据采集与训练入口

我把采集脚本里的 `--data-frequency > --camera-fps` 硬限制改成 warning。
这样我可以采集 50Hz 的 lowdim/action 数据，同时继续使用 30fps 相机。

当前采集命令：

```bash
python -m arx5_collector.scripts.collect_demo \
  --output data_local/arx5_glue_plus_50hz \
  --model X5 \
  --interface can1 \
  --usb-device 0 \
  --frequency 100 \
  --data-frequency 50 \
  --camera-pump-fps 30 \
  --preview-fps 5
```

我明确记录了 50Hz 的影响：

- action / lowdim 更密，可能缓解机械臂运动的段落感
- 相机仍是 30fps，所以会出现多个 action 样本共享同一帧图像
- 50Hz 更容易保留遥操作中的高频抖动，因此采集动作要更稳

训练命令使用：

```bash
python train.py \
  --config-name=train_diffusion_unet_arx5_hybrid_workspace \
  task.dataset_path=data_local/arx5_glue_plus_50hz \
  task.dataset.target_frequency=50 \
  task.dataset.val_ratio=0.0 \
  training.num_epochs=200 \
  dataloader.batch_size=16 \
  val_dataloader.batch_size=16 \
  dataloader.num_workers=4 \
  val_dataloader.num_workers=0 \
  val_dataloader.persistent_workers=False \
  training.checkpoint_every=50 \
  logging.mode=offline
```

我选择在快速部署实验阶段关闭 validation，把时间优先留给训练和实机测试。
这不是最终评估方案，只是用于快速验证 50Hz 是否能改善部署动作。

### 2. 我补强了部署安全逻辑

我继续优化了 `arx5_ckpt_loader/run_arx5_policy.py`，重点解决 policy 切入瞬间机械臂“埋头”和 action chunk 跳变问题。

现在部署逻辑包含：

- 切到 policy 时先把目标锚定到当前实际位姿
- 清空并重新填满 observation buffer
- `policy-start-hold-time` 时间内只保持当前位姿，不执行 policy 输出
- 第一个 action chunk 的第一个 waypoint 强制接当前位姿
- 保留 action 平滑限速：
  - `--max-action-pos-step`
  - `--max-action-rot-step`
  - `--max-action-gripper-step`

我没有在最终代码里保留绝对 z 限位，因为我希望开局位姿由我自己手动调整，而不是用硬编码高度把行为限制死。

推荐部署命令：

```bash
python -m arx5_ckpt_loader.run_arx5_policy \
  --ckpt data/outputs/YYYY.MM.DD/RUN/checkpoints/latest.ckpt \
  --model X5 \
  --interface can1 \
  --usb-device 0 \
  --device cuda:0 \
  --execute \
  --frequency 50 \
  --steps-per-inference 4 \
  --command-latency 0.12 \
  --policy-start-hold-time 1.0 \
  --max-action-pos-step 0.002 \
  --max-action-rot-step 0.008 \
  --max-action-gripper-step 0.0015
```

### 3. 我把 ACT 扩展成当前项目自己的包

我在总目录下新建了：

```text
arx5_act/
```

这个包不是直接改原始 ACT 训练脚本，而是做一个 ARX5 专用适配层，直接复用我现在采集的数据结构：

```text
data_local/<dataset>/
  replay_buffer.zarr
  videos/<episode>/0.mp4
  videos/<episode>/1.mp4
  videos/<episode>/2.mp4
```

ACT 里我定义：

```text
qpos   = [x, y, z, rx, ry, rz, gripper_width]
action = [x, y, z, rx, ry, rz, gripper_width]
```

我新增了：

```text
arx5_act/dataset.py
arx5_act/train_act.py
arx5_act/inspect_dataset.py
arx5_act/run_act_policy.py
arx5_act/README.md
```

并做了这些兼容：

- 原始 ACT 默认 ALOHA 双臂 `state_dim=14`，我改成支持 `state_dim=7`
- 修正 ACT 内部写死的 14 维 Linear 层
- 默认不下载 ResNet 预训练权重，避免离线环境报错
- 支持 `--device auto/cuda/cpu`
- 保留未来部署骨架，后续复用 DP 的相机和 ARX5 控制接口

检查命令：

```bash
python -m arx5_act.inspect_dataset \
  --dataset-path diffusion_policy-main/data_local/arx5_glue_plus_50hz \
  --chunk-size 50
```

已验证输出：

```text
samples: 22190
image: shape=(3, 3, 240, 320)
qpos: shape=(7,)
action: shape=(50, 7)
is_pad: shape=(50,)
```

ACT 训练命令：

```bash
python -m arx5_act.train_act \
  --dataset-path diffusion_policy-main/data_local/arx5_glue_plus_50hz \
  --ckpt-dir act_outputs/arx5_glue_plus_50hz_act \
  --num-epochs 50 \
  --batch-size 16 \
  --chunk-size 50 \
  --checkpoint-every 10 \
  --device auto
```

### 4. 我对当前项目定位的更新

今天之后，我不再把这个工作描述成“依赖之前某个老版本 DP”。

更准确的说法是：

> 我基于 ARX5 SDK 和 Diffusion Policy 的核心思想，搭建了自己的 ARX5 数据采集、数据处理、训练和部署闭环。当前项目使用我自己的三相机采集格式、ARX5 低维状态、7维 action 定义、50Hz 高频数据实验、部署端 action chunk 安全调度，并且已经开始扩展 ACT 作为第二条策略路线。

当前项目已经形成两条路线：

```text
ARX5 Diffusion Policy:
  arx5_collector -> arx5_image_dataset -> train.py -> arx5_ckpt_loader

ARX5 ACT:
  arx5_collector -> arx5_act.dataset -> arx5_act.train_act -> arx5_act.run_act_policy
```

### 5. 下一步

明天继续：

- 检查 50Hz DP 的训练 ckpt
- 测试 50Hz 部署是否比 20Hz 更平滑
- 优先试中间 ckpt，不只看 latest
- 如果 DP 夹取前仍然抖，补采更多“接近 + 对准 + 闭合”短 episode
- 开始 ACT 训练，并对比 ACT 和 DP 在同一套 ARX5 数据上的实机效果

## 2026-07-10

### 1. 我处理了 `glue_motion` 训练数据的视频/低维对齐问题

我在训练 `data_local/glue_motion` 时遇到 cache 构建报错：

```text
Video data_local/glue_motion/videos/12/0.mp4 produced 189 frames,
but episode 12 needs 200.
```

问题不是训练代码本身，而是部分 episode 的低维/action 步数比视频可解码帧数多。这样会导致后面的 action 找不到对应图像。

我一开始做了一个 `glue_motion_train_clean`，把坏 episode 整段过滤掉。但这样会丢掉太多数据。后来我改成更合理的做法：

```text
保留所有 episode
只裁掉视频覆盖不到的尾部低维/action 样本
原始 mp4 和 replay_buffer.zarr 不修改
裁剪只发生在训练 cache 构建阶段
```

我修改了：

```text
diffusion_policy-main/diffusion_policy/dataset/arx5_image_dataset.py
```

最终检查结果：

```text
10507 -> 10294 steps
72 / 72 episodes kept
Training readiness: PASS
```

这说明我没有再整段删除 episode，只丢掉了没有可靠图像对应的尾部样本。

### 2. 我做了 DP 训练结束后自动接 ACT 训练的脚本

为了让我离开电脑后可以自动连续训练，我新增了：

```text
scripts/train_dp_then_act.sh
```

这个脚本的逻辑是：

```text
1. source activate_arx5_env.sh
2. 训练 DP
3. 如果 DP 正常结束，再训练 ACT
4. 如果 DP 报错，ACT 不启动
5. DP 和 ACT 日志分开放到 logs/<RUN_NAME>/
6. ACT ckpt 放到 act_outputs/<RUN_NAME>_act/
```

我修复了两个脚本运行问题：

- `logs/` 不存在时，`nohup` 重定向会直接失败
- `conda activate` 期间 libxml2 的 deactivate hook 会触发 `set -u` 未定义变量错误

最终启动命令：

```bash
cd /media/star/Elyos_PSSD/ARX5/CY_arx5_dp
mkdir -p logs

nohup env \
  RUN_NAME=glue_motion_dp_act \
  DATASET_PATH=data_local/glue_motion \
  DP_EPOCHS=200 \
  ACT_EPOCHS=50 \
  ./scripts/train_dp_then_act.sh \
  > logs/glue_motion_dp_act_nohup.log 2>&1 &
```

查看日志：

```bash
tail -f /media/star/Elyos_PSSD/ARX5/CY_arx5_dp/logs/glue_motion_dp_act_nohup.log
```

目前 DP 已经正常进入训练：

```text
Training epoch 0
Loading cached ARX5 ReplayBuffer from disk.
```

### 3. 我检查并补强了 ACT 数据集入口

我担心 ACT 后续自动启动后会遇到和 DP 一样的视频尾帧不足问题，所以检查了：

```text
arx5_act/dataset.py
arx5_act/train_act.py
arx5_act/inspect_dataset.py
```

ACT 原来直接读取原始 `replay_buffer.zarr + videos/`，没有复用 DP 的视频对齐裁剪逻辑。我给 ACT dataset 也补了视频对齐保护：

```text
ACT video alignment trim
只裁掉视频实际覆盖不到的尾部 sample
不修改原始数据
```

检查命令：

```bash
python -m arx5_act.inspect_dataset \
  --dataset-path diffusion_policy-main/data_local/glue_motion \
  --chunk-size 50
```

检查结果：

```text
samples: 13748
image: shape=(3, 3, 240, 320)
qpos: shape=(7,)
action: shape=(50, 7)
is_pad: shape=(50,)
```

ACT 当前可以进入训练，但它还没有 DP 部署路线验证充分。后续需要单独实机对比。

### 4. 我排查了 GPU 占用看起来很低的问题

我看到 GPU 利用率只有很低的数值。检查后确认 DP config 里是：

```text
training.device: cuda:0
```

训练日志里速度大约：

```text
Training epoch 0: 5.5~5.9 it/s
```

这说明训练大概率已经在 GPU 上跑。GPU 利用率看起来低，主要可能是：

- `nvidia-smi` 显示的是瞬时利用率
- validation 阶段 CPU dataloader / 图像读取占比高
- batch size 和数据加载让 GPU 有等待

后续如果要优化速度，可以考虑：

```text
DP_NUM_WORKERS=8
减少 validation 频率
增大 batch size
```

### 5. 我优化了 DP 部署层，针对机械臂动作顿感做了改动

我标出了当前 DP 部署控制链路：

```text
diffusion_policy-main/arx5_ckpt_loader/run_arx5_policy.py
  相机 + 机器人状态
  -> Arx5ObsBuffer
  -> policy.predict_action
  -> action chunk 后处理
  -> Arx5Robot.schedule_waypoints

diffusion_policy-main/arx5_collector/robot/arx5_robot.py
  schedule_waypoint
  schedule_waypoints
  -> ARX5 SDK set_eef_cmd / set_eef_traj
```

我参考旧版 DP 部署方式后，认为当前顿感主要可能来自：

```text
1. 每个 chunk 都重新提交未来轨迹，可能覆盖上一段还没执行完的轨迹
2. 默认 command_latency 偏大，chunk 之间可能出现等待
3. 使用 target pose 做 anchor 时，如果 target 已经跑到未来，会和 actual pose 拉开
4. chunk 边界没有充分渐入，下一次预测的第一步可能和上一段末尾不连续
```

我做了这些改动：

```text
默认 replan-mode: receding -> after_chunk
默认 timestamp-mode: now -> obs
默认 command-latency: 0.15 -> 0.03
默认 action-exec-latency: 0.01 -> 0.02
默认 action-anchor: target -> actual
默认 boundary-blend-steps: 0 -> 3
新增 replan-lookahead: 0.12
chunk prepend current action 后，也会对后续几步做渐入
schedule_waypoints 会过滤非递增 target_times
```

这样部署时更接近旧版 DP 的节奏：

```text
基于观测时间戳生成动作时间表
一次提交一段连续 action chunk
在 chunk 末尾前一点点开始下一次推理
用实际当前位姿作为新 chunk 的连接点
```

语法检查已通过：

```bash
python -m py_compile \
  diffusion_policy-main/arx5_ckpt_loader/run_arx5_policy.py \
  diffusion_policy-main/arx5_collector/robot/arx5_robot.py \
  arx5_act/dataset.py
```

后续部署时可以先用默认参数试，如果仍然有顿感，再重点调：

```text
--steps-per-inference
--replan-lookahead
--boundary-blend-steps
--max-action-pos-step
--max-action-rot-step
```

### 6. 下一步

当前训练先不要动。等 DP + ACT 自动训练完成后，我要做：

- 先用新的 DP 部署默认参数 dry-run 看 chunk 是否连续
- 再 `--execute` 实机测试动作顿感是否降低
- 如果仍然顿，记录 policy chunk 的 `dt0/dtN/pose0/gripper_minmax`
- 对比 DP 和 ACT 在同一套 `glue_motion` 数据上的实机表现
- 如果 ACT 能稳定训练，再补 ACT 的部署日志和实机安全参数

### 7. 我把 DP 部署层继续拆成了两个独立模块

#### 7.1 本地 trajectory buffer

我新增了一个本地动作缓冲执行层：

```text
diffusion_policy-main/arx5_ckpt_loader/trajectory_buffer.py
```

原来的部署方式是：

```text
policy 预测一段 action chunk
-> 直接把整段 chunk 提交给 SDK
-> 下一次 policy 输出再提交下一段 chunk
```

这可能导致后一段 chunk 覆盖前一段还没执行完的轨迹，从而出现机械臂“抽一下”“闪一下”的效果。

现在新增的 buffer 模式是：

```text
policy 预测一段 action chunk
-> 写入本地 ActionTrajectoryBuffer
-> 后台 BufferedActionExecutor 以固定频率从 buffer 中插值采样
-> 连续发送单个 waypoint 给 ARX5 SDK
```

也就是把“模型预测频率”和“机械臂执行频率”分开：

```text
模型可以低频推理
执行层保持 100Hz 左右连续输出
新 chunk 只替换未来轨迹，不硬切当前执行点
```

我保留了原始 direct 模式，方便对比：

```text
--execution-layer direct
--execution-layer buffer
```

#### 7.2 部署轨迹日志

我新增了部署轨迹 JSONL 日志：

```text
diffusion_policy-main/arx5_ckpt_loader/trajectory_logger.py
```

它会记录：

```text
mode_switch: human / policy 切换
policy_chunk: 模型输出的 chunk、实际执行 chunk、时间戳、当前 actual pose
buffer_insert: chunk 插入 buffer 后的长度和未来 horizon
buffer_sample: buffer 实际喂给 SDK 的连续 waypoint
```

这样后续再出现抖动时，可以区分：

```text
模型输出本身跳
chunk 后处理导致跳
buffer 插值/执行导致跳
SDK 执行跟不上导致跳
```

#### 7.3 测试命令

先 dry-run，只看 policy 输出和日志，不执行机械臂：

```bash
python -m arx5_ckpt_loader.run_arx5_policy \
  --ckpt data/outputs/2026.07.09/21.36.16_train_diffusion_unet_arx5_hybrid_arx5_image/checkpoints/latest.ckpt \
  --model X5 \
  --interface can1 \
  --usb-device 0 \
  --device cuda:0 \
  --execution-layer buffer \
  --command-mode cmd \
  --trajectory-log data_local/policy_logs/buffer_dryrun.jsonl
```

确认画面、obs、policy chunk 都正常后，再实机执行：

```bash
python -m arx5_ckpt_loader.run_arx5_policy \
  --ckpt data/outputs/2026.07.09/21.36.16_train_diffusion_unet_arx5_hybrid_arx5_image/checkpoints/latest.ckpt \
  --model X5 \
  --interface can1 \
  --usb-device 0 \
  --device cuda:0 \
  --execute \
  --execution-layer buffer \
  --command-mode cmd \
  --buffer-frequency 100 \
  --trajectory-log data_local/policy_logs/buffer_execute.jsonl
```

如果 buffer 模式变差，可以立刻切回原来的 direct 模式：

```bash
--execution-layer direct
```

语法检查已通过：

```bash
/home/star/mambaforge/envs/arx5-dp/bin/python -m py_compile \
  diffusion_policy-main/arx5_ckpt_loader/run_arx5_policy.py \
  diffusion_policy-main/arx5_ckpt_loader/trajectory_buffer.py \
  diffusion_policy-main/arx5_ckpt_loader/trajectory_logger.py
```

## 2026-07-11

### 1. 我修复了 DP 结束后 ACT DataLoader worker 被 kill 的问题

现象：

```text
========== DP training finished ==========
========== ACT training started ==========
RuntimeError: DataLoader worker is killed by signal: Killed
```

判断原因：

```text
ACT dataset 当前直接从 videos/*.mp4 读取图像
num_workers=4 时，每个 worker 都会独立打开视频并解码
多进程视频解码容易造成内存/共享内存压力
系统直接 kill 掉某个 worker
```

我做的改动：

```text
arx5_act/train_act.py
  ACT 默认 batch_size: 16 -> 8
  ACT 默认 num_workers: 4 -> 0
  默认关闭 pin_memory
  只有 num_workers > 0 时才启用 persistent_workers / prefetch_factor

scripts/train_dp_then_act.sh
  ACT_BATCH_SIZE 默认 16 -> 8
  ACT_NUM_WORKERS 默认 4 -> 0
  新增 SKIP_DP=1 / SKIP_ACT=1 开关
```

这样 ACT 会慢一点，但更稳。后面如果确认稳定，再逐步尝试：

```text
ACT_NUM_WORKERS=1
ACT_BATCH_SIZE=8
```

由于 DP 已经训练完成，ACT 失败后不需要重跑 DP。只重跑 ACT 可以用：

```bash
cd /media/star/Elyos_PSSD/ARX5/CY_arx5_dp
mkdir -p logs

nohup env \
  RUN_NAME=glue_motion_dp_act \
  DATASET_PATH=data_local/glue_motion \
  SKIP_DP=1 \
  ACT_EPOCHS=50 \
  ACT_BATCH_SIZE=8 \
  ACT_NUM_WORKERS=0 \
  ./scripts/train_dp_then_act.sh \
  > logs/glue_motion_act_retry_nohup.log 2>&1 &
```

### 2. 我对比了学长项目里的 ARX5 kp/kd，并修复了当前部署层 gain 没启用的问题

我检查了：

```text
/home/star/arx_lqy_new/arx5-sdk/include/app/config.h
当前项目 arx5-sdk-main/include/app/config.h
```

两边 SDK 的 X5 cartesian controller 默认参数是一致的：

```text
cartesian_controller_6:
  kp = [200, 200, 200, 120, 80, 60]
  kd = [5, 5, 5, 1, 1, 1]

cartesian_controller_7:
  kp = [300, 300, 300, 300, 80, 50, 40]
  kd = [5, 5, 5, 5, 1, 1, 1]
```

但是 SDK 初始化时默认会进入 damping：

```text
kp = 0
kd = default_kd
```

我之前的本地配置只覆盖了夹爪：

```text
gripper_kp = 5.2
gripper_kd = 0.8
```

没有显式把机械臂本体 kp/kd 切到 SDK default。这可能导致部署时末端追踪目标很软，表现成卡顿、抖动或者一帧一帧追目标。

我做的改动：

```text
arx5-sdk-main/python/arx5_local_config.py
  新增 ARX5_ARM_GAIN_MODE
  新增 ARX5_ARM_KP_SCALE
  新增 ARX5_ARM_KD_SCALE
  apply_local_controller_gain 会把 arm kp/kd 设置为 SDK cartesian default

diffusion_policy-main/arx5_collector/robot/arx5_robot.py
  新增 arm_gain_mode / arm_kp_scale / arm_kd_scale
  新增 get_gain_summary

diffusion_policy-main/arx5_ckpt_loader/run_arx5_policy.py
  新增 --arm-gain-mode default/damping
  新增 --arm-kp-scale
  新增 --arm-kd-scale
  启动时打印实际 kp/kd
```

部署时先用：

```bash
--arm-gain-mode default \
--arm-kp-scale 1.0 \
--arm-kd-scale 1.0
```

如果还是抖，但不是乱飞，可以优先尝试增加阻尼：

```bash
--arm-kd-scale 1.5
```

如果动作太硬或有过冲，可以尝试降低刚度：

```bash
--arm-kp-scale 0.8
```

### 3. 我把 reset 改成回到部署启动时的初始位姿，并加了 policy deadband

现象：

```text
调 kp/kd 后机械臂效果明显变好
但是 r reset 回不到一开始手动摆好的位置
policy 执行时仍然有轻微颤抖
```

原因：

```text
之前 r 调用的是 SDK reset_to_home()
它回的是 SDK home，不是本次部署开始时的初始位姿
```

我做的改动：

```text
diffusion_policy-main/arx5_ckpt_loader/run_arx5_policy.py
  启动后记录 session_initial_pose 和 session_initial_gripper
  新增 --reset-target session/home
  新增 --reset-duration
  r 默认回到 session 初始位姿
  新增 --action-pos-deadband
  新增 --action-rot-deadband
  新增 --action-gripper-deadband
```

默认：

```text
--reset-target session
```

后续我又把夹爪 reset 目标单独拆出来：

```text
--reset-gripper-target sdk/session/current/open/close
```

默认：

```text
--reset-target session
--reset-gripper-target sdk
```

含义是：

```text
机械臂本体回到部署启动时记录的 session_initial_pose
夹爪回到 SDK reset_to_home 的默认状态，也就是 fully open
```

如果想恢复 SDK home：

```bash
--reset-target home
```

轻微颤抖可以先试：

```bash
--action-pos-deadband 0.001 \
--action-rot-deadband 0.003 \
--action-gripper-deadband 0.0005
```

这些 deadband 的作用是：如果 policy 输出的相邻 action 变化小于阈值，就保持上一点不变，减少很小的来回抖动。

## 22. 2026-07-12 到 2026-07-13 数据扩展、三模型训练与 ARX5 部署层优化

这几天我主要围绕三个方向继续推进：

```text
1. 扩展数据集，让训练覆盖胶棒放在边缘位置的情况
2. 把 DP、ACT-EFF、ACT-Joint 三条训练链路整理成可复用脚本
3. 按 ARX5 SDK 的实际控制逻辑继续优化部署层，而不是照搬 UR5 的执行方式
```

### 1. 我发现 ACT 泛化区域比采集区域小

### 问题

ACT 部署时能完成部分夹取任务，但有效工作区域比采集区域小很多：

```text
胶棒放在采集区域中心附近，ACT 有机会完成任务
胶棒放在左侧 / 右侧边缘时，末端会朝对应方向偏一点，但偏移量不够
远处位置也容易出现“夹空气”
```

我通过打印 ACT raw chunk 和 temporal action 发现：

```text
policy 输出确实会随着物体位置发生变化
但变化幅度不够，动作没有覆盖到物体真实位置
```

### 判断

这不完全是部署层问题。因为：

```text
1. 我把胶棒放左边时，policy 输出 y / pose 确实会变
2. 轻微人工外力把末端压低后，ACT 能继续完成夹取
3. 同一套数据 DP 效果更好，说明基础数据格式没有完全错
```

更可能的问题是：

```text
ACT 对边缘位置的数据覆盖不足
ACT 训练样本中中心区域占比更高
ACT 当前学的是 EEF 位姿，可能对真实机械臂 IK 和末端视觉偏差更敏感
```

### 解决方案

我重新采集了一组边缘数据，重点覆盖：

```text
胶棒偏左
胶棒偏右
胶棒偏远
胶棒靠近 workspace 边界
```

新数据集：

```text
data_local/glue_motion_new
```

数据审计结果显示：

```text
Suggested bad episodes: 0
```

说明新采数据没有明显坏 episode，可以进入后续合并和训练。

### 2. 我把旧中心数据和新边缘数据整理成 data_pro 数据包

### 问题

我希望保留之前能工作的中心区域数据，同时加入新采的边缘数据。如果直接混用多个数据目录，训练和 cache 管理会混乱。

### 解决方案

我做了一个数据合并工具，把多个数据包整理成一个新的正式数据包：

```text
diffusion_policy-main/arx5_collector/scripts/merge_data_pro.py
```

合并后的数据集：

```text
diffusion_policy-main/data_local/data_pro/glue_motion_edge_v2
```

合并时做的事情：

```text
重新编号 episode
复制 replay_buffer.zarr
复制 videos/<episode>/<camera>.mp4
写入 episode_source_map.tsv，记录每条 episode 来自哪个原始数据集
```

这样后续训练只需要指定一个 dataset path，不需要分别管理中心数据和边缘数据。

### 3. 我把 DP cache、ACT-EFF cache、ACT-Joint cache 分开管理

### 问题

DP 和 ACT 的 cache 结构不同：

```text
DP cache 用 Diffusion Policy 的 ReplayBuffer image cache
ACT cache 需要按 ACT 的 image/qpos/action 读取方式组织
ACT-EFF 和 ACT-Joint 的 qpos/action 语义不同，也不能混用同一个 metadata
```

如果 cache 放在数据集目录根部，后续会很难判断当前训练用的是哪个 cache。

### 解决方案

我把 cache 统一放到独立目录：

```text
diffusion_policy-main/data_local/cache/glue_motion_edge_v2/dp
diffusion_policy-main/data_local/cache/glue_motion_edge_v2/act
```

当前三个 cache 分别是：

```text
DP:
  data_local/cache/glue_motion_edge_v2/dp/arx5_0bbeaa68f6139889e9119b0d07a3318f.zarr.zip

ACT-EFF:
  data_local/cache/glue_motion_edge_v2/act/act_eef_cache_a70a8ebb8f0d5c2c5b95f643d09d629a.zarr

ACT-Joint:
  data_local/cache/glue_motion_edge_v2/act/act_joint_cache_03af134a6b9cfdc16cc93bff733f0149.zarr
```

我也修改了 DP dataset：

```text
diffusion_policy-main/diffusion_policy/dataset/arx5_image_dataset.py
```

新增：

```text
cache_dir
cache_path
```

这样训练时可以显式指定 cache 路径。如果显式 cache 不存在，会直接报错，而不是偷偷新建一个新的 hash cache，避免误用错误数据。

### 4. 我做了三模型训练脚本

### 目标

我希望一次启动后按顺序训练：

```text
1. DP
2. ACT-EFF
3. ACT-Joint
```

并且三个模型的 ckpt 和日志分开保存。

### 解决方案

新增脚本：

```text
scripts/train_three_models.sh
```

输出目录：

```text
DP:
  diffusion_policy-main/data/outputs/manual/<RUN_NAME>/dp

ACT-EFF:
  act_outputs/<RUN_NAME>/eef

ACT-Joint:
  act_outputs/<RUN_NAME>/joint
```

日志目录：

```text
logs/<RUN_NAME>/
  dp_train.log
  act_eef_train.log
  act_joint_train.log
```

当前训练任务：

```text
RUN_NAME=glue_motion_edge_v2_three_200
DATASET_PATH=data_local/data_pro/glue_motion_edge_v2
TARGET_FREQUENCY=20
DP_EPOCHS=200
ACT_EPOCHS=200
DP_BATCH_SIZE=16
ACT_BATCH_SIZE=16
```

当前状态：

```text
DP 已完成 200 epoch
ACT-EFF 正在训练
ACT-Joint 会在 ACT-EFF 正常结束后启动
```

### 5. 我关闭或弱化了 validation 来加快实验

### 问题

训练时 validation 和 train 耗时接近，导致整体训练时间翻倍。当前阶段我更想先快速得到 ckpt 做实机对比。

### 解决方案

DP 训练脚本中默认设置：

```text
DP_VAL_EVERY=1000000
DP_SAMPLE_EVERY=1000000
```

ACT 训练入口新增：

```text
--val-every
```

当：

```text
--val-every 0
```

时，ACT 跳过 validation，并用 train loss 保存 `policy_best.ckpt`。

### 说明

这只是快速实机实验策略，不代表最终模型评估不需要 validation。正式对比模型时，仍然应该保留一部分 episode 做验证。

### 6. 我把 ACT 扩展成 EEF 和 Joint 两种训练模式

### 问题

学长建议 ACT 可以尝试学习关节位姿，因为原版 ACT 更接近关节空间控制。我当前 ACT-EFF 学的是：

```text
qpos   = [x, y, z, rx, ry, rz, gripper_width]
action = [x, y, z, rx, ry, rz, gripper_width]
```

但 ACT 原版更偏向：

```text
qpos   = joint position
action = future joint position
```

### 解决方案

我在 ACT dataset 中加入：

```text
state_mode=eef
state_mode=joint
```

对应关系：

```text
EEF:
  qpos   = robot0_eef_pos + robot0_eef_rot_axis_angle + robot0_gripper_width
  action = DP 7D EEF action

Joint:
  qpos   = robot_joint + robot_gripper
  action = future robot_joint + robot_gripper
```

相关文件：

```text
arx5_act/dataset.py
arx5_act/train_act.py
arx5_act/policy_utils.py
arx5_act/build_cache.py
```

### 当前限制

ACT-Joint 现在可以训练，但在线部署还没有接入 joint-space 执行器。当前 `arx5_act/run_act_policy.py` 仍然是 EEF 执行路径。因此：

```text
ACT-EFF 可以实机部署
ACT-Joint 先用于训练对比和离线分析
ACT-Joint 实机部署需要后续补 ARX5 joint controller 执行层
```

### 7. 我修复了 ACT 训练慢和 DataLoader worker 被 kill 的问题

### 问题

ACT 训练最开始很慢，而且出现过：

```text
RuntimeError: DataLoader worker exited unexpectedly
```

原因是 ACT 原本训练时随机 seek 三路 mp4，CPU 解码和内存压力都很大。

### 解决方案

我给 ACT 加了 zarr 图像 cache：

```text
arx5_act/build_cache.py
arx5_act/build_cache_from_dp.py
arx5_act/dataset.py
```

训练时直接从 zarr 读取图像，不再每个 sample 去 seek mp4。

同时训练参数使用：

```text
ACT_BATCH_SIZE=16
ACT_NUM_WORKERS=0
ACT_DEVICE=cuda
ACT_USE_CACHE=1
```

当前 ACT 训练速度已经从非常慢提升到可接受水平。

### 8. 我把 DP 部署层拆成更工程化的结构

### 问题

DP 部署文件 `run_arx5_policy.py` 之前堆了太多逻辑：

```text
相机读取
键盘状态机
policy inference
action chunk 后处理
轨迹 buffer
reset
安全保护
```

后续调参数很难定位。

### 解决方案

我新增了部署子模块：

```text
diffusion_policy-main/arx5_ckpt_loader/deployment/action_postprocess.py
diffusion_policy-main/arx5_ckpt_loader/deployment/reset.py
diffusion_policy-main/arx5_ckpt_loader/deployment/arx_runtime.py
diffusion_policy-main/arx5_ckpt_loader/trajectory_buffer.py
```

职责划分：

```text
action_postprocess.py:
  chunk 边界融合
  单步限幅
  deadband
  anchor action 生成

reset.py:
  session pose reset
  多次尝试
  settle hold
  reset 误差检查

arx_runtime.py:
  policy 动作安全调度
  tracking guard hold

trajectory_buffer.py:
  新旧 chunk 融合
  高频本地插值执行
```

### 9. 我按 ARX5 SDK 的控制逻辑调整了 DP/ACT 部署默认值

### 问题

UR5 常见部署方式更像外部 servo loop，不断给机械臂发送目标位姿。而 ARX5 SDK 的 cartesian controller 内部有自己的插值器：

```text
set_eef_cmd:
  覆盖单点目标

set_eef_traj:
  覆盖未来轨迹，并在 SDK 内部计算 joint velocity
```

如果我把 ARX5 当 UR5 一样不断塞未处理的整段未来轨迹，可能出现：

```text
后一段 policy chunk 挤掉前一段未来轨迹
动作像一帧一帧执行
target 和 actual 拉开后继续追错误目标
```

### 解决方案

我把当前 ARX5-friendly 默认部署改成：

```text
--execution-layer buffer
--command-mode traj
--command-latency 0.05
--buffer-frequency 100
--buffer-blend-time 0.12
--arm-kp-scale 1.5
--arm-kd-scale 0.5
--tracking-guard
```

含义：

```text
1. policy 仍然输出 chunk
2. 本地 buffer 负责新旧 chunk 融合
3. 100Hz executor 从 buffer 中采样一个短预瞄目标
4. 每次用 set_eef_traj([单点]) 交给 ARX5 SDK
5. SDK 内部插值器负责更平滑的 joint velocity
```

这比直接整段覆盖 SDK 未来轨迹更适合 ARX5。

### 10. 我加入了 tracking guard，防止 policy 追飞

### 问题

ARX5 部署中如果 IK 失败、目标跳变、或者控制跟踪落后，可能出现：

```text
TargetTCPPose 和 ActualTCPPose 差距越来越大
policy 继续输出新的目标
机械臂表现为突然跳、抖、追不上
```

### 解决方案

我在 ARX5 robot wrapper 中加入：

```text
tracking_pos_error_limit
tracking_rot_error_limit
tracking_is_healthy()
tracking_error()
```

默认：

```text
--tracking-pos-error-limit 0.08
--tracking-rot-error-limit 0.6
```

如果超过阈值：

```text
不继续追 policy 输出
临时 hold 当前 ActualTCPPose
打印 tracking guard hold
```

这个保护接入了：

```text
DP buffer executor
DP direct chunk schedule
ACT policy 单点执行路径
```

人工 SpaceMouse 不走这个限制，方便我随时接管。

### 11. 我把 ACT 部署层也工程化并对齐 DP 操作流

### 问题

ACT 部署入口原来和 DP 重复了很多代码：

```text
重复的 clamp_action_chunk_delta
重复的 reset_to_pose
temporal aggregation 写在主循环里
runtime gain 没有和 DP 一样显式打印
```

### 解决方案

我新增：

```text
arx5_act/deployment/temporal.py
```

把 ACT temporal aggregation 拆成：

```text
TemporalActionAggregator
```

ACT 现在复用 DP 的：

```text
action_postprocess.py
reset.py
arx_runtime.py
```

ACT 默认部署参数也改为：

```text
--command-mode traj
--command-latency 0.05
--arm-kp-scale 1.5
--arm-kd-scale 0.5
--tracking-guard
--temporal-agg
--temporal-agg-k 0.01
```

操作流与 DP 保持一致：

```text
human mode: SpaceMouse 控制
c: 切到 policy
h: 回到 human
r: reset
q: 退出
```

### 12. 我记录了后续部署调参顺序

后续不要一上来同时改很多参数。当前更合理的顺序是：

```text
1. 先用默认 ARX5-friendly 部署参数测试
2. 如果动作仍然一帧一帧，优先调 buffer-blend-time
3. 如果动作太钝，再减小 buffer-blend-time
4. 如果围绕目标来回震荡，再调 kd
5. 如果动作过硬或过冲，再调 kp
```

推荐优先比较：

```bash
--buffer-blend-time 0.12
--buffer-blend-time 0.16
--buffer-blend-time 0.08
```

PID 只比较少数组合：

```bash
--arm-kp-scale 1.5 --arm-kd-scale 0.5
--arm-kp-scale 1.5 --arm-kd-scale 0.7
--arm-kp-scale 1.2 --arm-kd-scale 0.5
```

如果出现 tracking guard 频繁触发，说明不是单纯 PID 问题，而是：

```text
policy 输出跳变过大
目标不可达或 IK 不稳定
执行层时间戳/延迟不合适
机械臂实际跟踪已经落后
```

这时应该先看部署轨迹日志，而不是继续盲目加大 kp。

### 13. 当前阶段总结

截至目前，我已经形成了比较完整的 ARX5 策略学习闭环：

```text
采集:
  SpaceMouse + ARX5 SDK + 三相机

数据:
  old ARX5 DP schema
  camera_0=USB
  camera_1/2=RealSense
  replay_buffer.zarr + videos

训练:
  DP
  ACT-EFF
  ACT-Joint

部署:
  DP online policy
  ACT-EFF online policy
  human / policy / reset 操作流统一
  ARX5 tracking guard
  ARX5 SDK traj-style short preview execution
```

当前最重要的后续验证是：

```text
1. 等 ACT-EFF 训练完成
2. 先用新的 ARX5-friendly DP 部署层测试 latest.ckpt
3. 再用 ACT-EFF 测试同一任务
4. 对比 DP 和 ACT 在边缘位置、远处位置、中心位置的成功率
5. 如果 ACT-Joint 训练效果值得继续，再补 joint-space 在线执行器
```

## 2026-07-15：我开始基于 SAIL 论文改造 DP-CFG 版本

### 1. 我把原版 DP 和 CFG 实验版本分开

我之前把 CFG / prev_action 逻辑接进了 DP 部署入口。为了避免污染已经跑通的原版 DP，我把部署入口拆开：

```text
原版 DP:
  diffusion_policy-main/arx5_ckpt_loader/run_arx5_policy.py
  scripts/run_dp_pro.sh

CFG / EAG 实验版:
  arx5_dp_cfg/run_arx5_cfg_policy.py
  arx5_dp_cfg/deployment/cfg_prev_action.py
  scripts/run_dp_cfg_pro.sh
```

这样我后续可以继续稳定使用原版 DP，同时在 `arx5_dp_cfg` 里独立测试 SAIL 风格的 chunk consistency。

### 2. 我修正了 prev_action 的定义

我一开始把 `prev_action` 理解成历史动作，也就是当前 observation 之前已经发生的动作。后面读 SAIL 论文后确认，这不是论文里 CFG condition 的含义。

论文中的 action condition 是：

```text
ac = 上一段 policy chunk 中还没有执行、但接下来会继续执行的未来 tail
```

所以我把数据集里的 `prev_action` 改成：

```text
prev_action = 当前 action chunk 的前 Hc 步
prev_action_mask = 有效 condition mask
prev_action_mode = future
```

这等价于训练时用 teacher-forced 的未来 action condition，让网络学习：

```text
在给定 obs 的情况下，生成 action chunk 时要和已计划的未来 tail 保持一致
```

我用检查脚本验证了对齐关系：

```text
mean_condition_pose_error=0.000000
```

说明 `prev_action` 和当前 action chunk 前几步严格对齐。

### 3. 我把 CFG 真正加进 U-Net denoise 过程

之前的版本只是把 `prev_action` 编码成一个全局向量拼到 condition 里，本质上是 conditional DP，不是完整的 classifier-free guidance。

这次我在 `arx5_dp_cfg/diffusion_unet_hybrid_image_policy_cfg.py` 里加入了论文形式的 CFG blending：

```text
eps_guided = eps_uncond + w * (eps_cond - eps_uncond)
```

其中：

```text
w = 0:
  不使用 prev_action，等价 unconditional

w = 1:
  普通 conditional

w > 1:
  更强的 consistency guidance
```

训练时仍然通过 `prev_chunk_dropout` 让模型见到空 condition，从而具备 unconditional 分支能力。

### 4. 我加入了 EAG 的部署逻辑

SAIL 论文里强调：不是任何时候都应该强行使用 CFG。  
如果当前实际 EEF 和 action condition 的 tracking error 太大，condition 可能已经和真实状态不匹配，此时继续 guidance 会把模型往错误方向拉。

所以我在部署层加入 EAG：

```text
如果 tracking error <= threshold:
  使用 cfg_guidance_weight

如果 tracking error > threshold:
  guidance_weight = 0
```

默认阈值参考论文：

```text
position threshold = 0.02 m
rotation threshold = 0.05 rad
```

后续部署时可以通过参数调整：

```bash
--cfg-guidance-weight 1.0
--eag-pos-threshold 0.02
--eag-rot-threshold 0.05
```

### 5. 我更新了 CFG 离线验证脚本

旧指标看的是历史动作边界误差，不适合新的 future condition。

现在验证脚本看：

```text
condition_consistency_improvement(empty-real)
```

含义是：

```text
使用真实 prev_action condition 后，预测 action 是否比空 condition 更贴近 condition tail
```

我跑 mini 训练后得到：

```text
valid_samples=32
prediction_diff_mean=0.672678
condition_consistency_improvement(empty-real): mean=0.061841 median=0.090125 positive_ratio=71.88%
gt_first_action_improvement(empty-real): mean=0.212915 median=0.183564 positive_ratio=84.38%
```

这个结果说明：

```text
1. prev_action condition 确实影响了输出
2. 输出方向是对的
3. 真实 condition 不仅提升 chunk consistency，也让首步 action 更接近 GT
```

这和之前错误定义 prev_action 时出现的负 improvement 相反，说明这次数据定义和 CFG 方向更合理。

### 6. 当前 CFG 版本完成程度

当前完成的是：

```text
DP + ARX5 部署
SAIL 风格 future action condition
U-Net denoise 内 CFG blending
基于 tracking error 的 EAG 开关
离线 consistency 验证脚本
原版 DP / CFG DP 部署隔离
```

还没有完整复现 SAIL 的部分：

```text
adaptive speed modulation
critical action / speed factor 预测
完整 action scheduling lower bound
SPARC / CON / LDLJ 等完整 smoothness 指标
针对 ARX5 的 EAG threshold 系统调参
```

所以当前阶段我把它定义为：

```text
ARX5-DP-CFG/EAG 原型版
```

不是完整 SAIL 复现，但已经具备论文中最核心的 chunk consistency 机制。

### 7. 下一步计划

下一步我应该：

```text
1. 用 glue_motion_base 或更稳定的数据正式训练 CFG 版本
2. 先跑 50 epoch，不直接上 200 epoch
3. 离线比较 w=1.0 / w=1.5 / w=2.0
4. 如果 consistency 提升且 GT 不变差，再上机械臂
5. 部署时先开 EAG，再对比 --no-eag
6. 如果 CFG 有效，再考虑补 speed modulation 和 smoothness metrics
```

## 2026-07-16：我继续调试 DP-CFG 部署时间线，并整理训练入口

### 1. 我跑了 CFG 正式训练并修正了启动目录问题

### 问题

我一开始在项目总目录直接运行：

```bash
python train.py \
  --config-dir ../arx5_dp_cfg \
  --config-name=train_diffusion_unet_arx5_hybrid_workspace_cfg \
  task.dataset_path=data_local/data_pro/glue_motion_edge_v2 \
  prev_chunk_dropout=0.25 \
  training.num_epochs=200 \
  dataloader.batch_size=16 \
  val_dataloader.batch_size=16 \
  dataloader.num_workers=4 \
  val_dataloader.num_workers=4 \
  training.checkpoint_every=25 \
  logging.mode=offline \
  hydra.run.dir=data/outputs/manual/cfg_glue_motion_edge_v2_dropout025_200
```

终端报错：

```text
python: can't open file '/media/star/Elyos_PSSD/ARX5/CY_arx5_dp/train.py': [Errno 2] No such file or directory
```

### 原因

`train.py` 不在项目总目录，而是在：

```text
diffusion_policy-main/train.py
```

同时 CFG 配置在总目录下的：

```text
arx5_dp_cfg/
```

所以训练时必须先进入 `diffusion_policy-main`，并把总目录加入 `PYTHONPATH`，否则 Hydra 可以找到配置但 Python 不一定能找到 CFG 自定义模块。

### 解决方案

我使用下面的正式训练入口：

```bash
cd /media/star/Elyos_PSSD/ARX5/CY_arx5_dp
source ./activate_arx5_env.sh

cd /media/star/Elyos_PSSD/ARX5/CY_arx5_dp/diffusion_policy-main
export PYTHONPATH=/media/star/Elyos_PSSD/ARX5/CY_arx5_dp:$PYTHONPATH

python train.py \
  --config-dir ../arx5_dp_cfg \
  --config-name=train_diffusion_unet_arx5_hybrid_workspace_cfg \
  task.dataset_path=data_local/data_pro/glue_motion_edge_v2 \
  prev_chunk_dropout=0.25 \
  training.num_epochs=200 \
  dataloader.batch_size=16 \
  val_dataloader.batch_size=16 \
  dataloader.num_workers=4 \
  val_dataloader.num_workers=4 \
  training.checkpoint_every=25 \
  logging.mode=offline \
  hydra.run.dir=data/outputs/manual/cfg_glue_motion_edge_v2_dropout025_200
```

训练输出目录为：

```text
diffusion_policy-main/data/outputs/manual/cfg_glue_motion_edge_v2_dropout025_200
```

我确认进程仍在运行：

```text
python train.py ... train_diffusion_unet_arx5_hybrid_workspace_cfg ...
```

需要注意：`.hydra/hydra.yaml` 只是 Hydra 配置文件，不是训练日志。查看训练进度应该看：

```bash
tail -f /media/star/Elyos_PSSD/ARX5/CY_arx5_dp/diffusion_policy-main/data/outputs/manual/cfg_glue_motion_edge_v2_dropout025_200/train.log
```

或者看 checkpoint：

```bash
ls -lh /media/star/Elyos_PSSD/ARX5/CY_arx5_dp/diffusion_policy-main/data/outputs/manual/cfg_glue_motion_edge_v2_dropout025_200/checkpoints
```

### 2. 我发现 CFG 部署时的核心问题不是模型，而是时间线没有对齐

### 问题

CFG 版本最初上机械臂时有明显的“回溯”和“闪现”：

```text
上一段 chunk 还没执行完
新 chunk 算出来后从旧 chunk 的某个早期点重新接管
机械臂表现为往回抽、前后抖、上下跳
```

即使把 `CFG_W=0`，也不等价于原版 DP，因为 CFG 模型结构和部署入口已经不同。  
所以不能用 `CFG_W=0` 来判断“原版 DP 效果”，原版 baseline 仍然应该用：

```bash
./scripts/run_dp_pro.sh
```

### 原因

SAIL/CFG 的 action condition 不是历史动作，而是：

```text
上一段 chunk 中还没有执行、但接下来正在被执行器继续执行的 future tail
```

部署时如果我只是从上一段 chunk 的固定下标取 tail，就会出现两个问题：

```text
1. 实际执行器已经走到更后面了，但 condition 还停留在旧下标
2. 新 chunk 生成后直接覆盖旧 chunk 未执行部分，导致时间点跳变
```

这会让机械臂在 chunk 边界处回到旧轨迹的前面，看起来就是“回溯”。

### 解决方案

我把 CFG 部署改成以执行器未来轨迹为准，而不是以 Python 里保存的 last chunk 下标为准。

当前逻辑是：

```text
1. continuous executor 持续执行上一段轨迹
2. policy 推理前，从 executor 当前未来轨迹中采样 prev_action condition
3. U-Net 用 obs + executor_future_condition 生成新 chunk
4. 根据 condition 覆盖长度 drop 掉新 chunk 前缀
5. 把剩下的执行段 + tail 提交给 executor
6. executor 保留旧轨迹到新 chunk handoff 时间，再替换未来轨迹
```

日志中正常状态应该能看到：

```text
source=executor_future
cfg prev valid=6
cfg_drop=6
submit_extra=6
exec_horizon≈0.6~0.7s
next_delay≈0.2s
```

这说明 CFG condition 来自真实执行器未来计划，而不是一个静态 last chunk。

### 3. 我加入了 future replacement 和 boundary blend

### 问题

即使 condition 对齐后，chunk 和 chunk 之间仍然可能有轻微跳变。原因是新 chunk 和旧 chunk 的未来段不是完全一样的。

如果直接替换：

```text
old_future -> new_future
```

在切换点仍然可能有速度或位置不连续。

### 解决方案

我在 continuous executor 里加入了：

```text
replace_future=True
replace_blend_time
```

含义：

```text
1. 切换点之前继续保留旧轨迹
2. 切换点之后才接入新轨迹
3. 新旧轨迹在 replace_blend_time 时间窗内做线性融合
```

当前我测试过比较可用的参数：

```bash
PREVIEW_TIME=0
REPLAN_LOOKAHEAD=0.45
REPLACE_BLEND_TIME=0.15
CFG_PREV_LATENCY=0.12
CFG_PREV_LATENCY_MARGIN=0.02
CFG_PREV_MAX_START_IDX=6
CFG_W=1
EAG_MODE=on
```

这组参数下，之前明显的前后时间摆动基本解决，动作可以连贯完成任务，但平滑程度仍然没有 ACT 那么自然。

### 4. 我确认 preview_time 能改善平滑，但不是 CFG 的根本解法

### 现象

把 `preview_time` 调大，例如 `0.1s`，机械臂会明显更平滑。

### 原因

`preview_time` 本质上是把新轨迹整体往未来推迟，让执行器有更长的缓冲时间：

```text
preview_time 越大:
  新轨迹越晚接管
  旧轨迹越有时间继续执行
  chunk 边界越不容易硬切
```

但这不是 CFG/SAIL 论文里的核心逻辑。  
如果只靠 preview_time，实际是在用“延后执行”掩盖时间对齐问题，代价是策略响应会变慢。

### 当前处理

我把 CFG 版本的核心目标定为：

```text
尽量在 PREVIEW_TIME=0 的情况下，通过 executor_future condition + replace_future + blend 解决边界跳变
```

当前 `preview_time=0` 已经可以跑通，但如果追求更稳的实际抓取，仍可以把它作为工程参数小幅打开。

### 5. 我处理了夹爪 torque safety 对动作的干扰

### 问题

部署日志中出现：

```text
Gripper torque is too large, gripper pos cmd is not updated
Gripper safety hold: requested=0.00154 actual=0.02406 safe=0.02606 torque=-0.885 limit=0.750
```

这说明模型在抓取阶段经常给出接近完全闭合的目标，例如：

```text
gripper ≈ 0.001~0.006 m
```

但真实夹爪已经接触物体，继续闭合会触发 torque safety。  
如果安全逻辑频繁介入，机械臂本体轨迹可能是平滑的，但夹爪动作会被 hold，任务表现会变得不稳定。

### 解决方案

我把 `scripts/run_dp_cfg_pro.sh` 加了可调的：

```bash
GRIPPER_MARGIN
```

并传给部署入口：

```bash
--gripper-margin "${GRIPPER_MARGIN}"
```

当前建议：

```bash
GRIPPER_MARGIN=0.012 ./scripts/run_dp_cfg_pro.sh
```

如果仍然频繁 torque hold，可以尝试：

```bash
GRIPPER_MARGIN=0.016 ./scripts/run_dp_cfg_pro.sh
```

代价是夹爪不会闭合到最小值，可能降低抓紧程度。  
所以这不是模型优化，而是 ARX5 硬件安全折中。

### 6. 我整理了当前 CFG 部署推荐命令

当前比较稳定的启动方式：

```bash
cd /media/star/Elyos_PSSD/ARX5/CY_arx5_dp

CKPT_PATH=diffusion_policy-main/data/outputs/2026.07.15/18.01.36_train_diffusion_unet_arx5_hybrid_arx5_image/checkpoints/latest.ckpt \
CFG_W=1 \
PREVIEW_TIME=0 \
REPLAN_LOOKAHEAD=0.45 \
REPLACE_BLEND_TIME=0.15 \
CFG_PREV_LATENCY=0.12 \
CFG_PREV_LATENCY_MARGIN=0.02 \
CFG_PREV_MAX_START_IDX=6 \
GRIPPER_MARGIN=0.012 \
EAG_MODE=on \
./scripts/run_dp_cfg_pro.sh
```

如果这组仍然有轻微顿感，优先按下面顺序调：

```text
1. REPLACE_BLEND_TIME: 0.15 -> 0.18
2. GRIPPER_MARGIN: 0.012 -> 0.016
3. CFG_W: 1.0 -> 0.7
4. 最后才考虑 PREVIEW_TIME: 0 -> 0.05/0.1
```

不要一开始就同时改多个参数，否则无法判断改善来自哪里。

### 7. 今天对 CFG 当前状态的判断

当前我已经完成：

```text
1. CFG 训练入口跑通
2. prev_chunk_dropout=0.25 正式训练启动
3. 部署层改成 executor_future condition
4. 修复 chunk 边界回溯
5. 加入 replace_future 和 replace_blend_time
6. 增加 gripper margin 来减少 torque safety 干扰
7. 整理了原版 DP 和 CFG DP 的独立运行方式
```

当前还没有完全解决：

```text
1. CFG 版本任务成功率仍可能低于最稳定的原版 DP
2. 动作平滑程度还没有达到 ACT 那种 temporal aggregation 的自然感
3. CFG_W / EAG threshold / blend time 还没有系统化 sweep
4. 夹爪闭合策略还需要结合任务成功率继续调
```

所以我现在对项目状态的判断是：

```text
原版 DP:
  已经可以作为稳定 baseline

ACT:
  已经能跑，但泛化区域和动作模式仍需要数据/模型继续优化

CFG-DP:
  时间对齐问题已经基本修好，可以继续做正式训练和参数 sweep，
  但还不能说已经超过原版 DP。
```

---

# 2026-07-22 ~ 2026-07-24 调试日志

## 1. 我重新整理了当前 DP / ACT / CFG 的实验定位

### 当前判断

这几天我把三个方向的定位重新梳理了一遍：

```text
原版 DP-EEF:
  当前最稳定的 baseline，尤其是 2026.07.10 glue_motion 训练出的版本效果最好。

DP-Joint:
  能识别胶棒位置，某些情况下泛化区域比 EEF 更大，
  但动作会放大我的人工操作习惯，容易出现夹空气、重复低头夹取、动作不够自然。

ACT-Joint:
  joint 表达下动作更贴近 ACT 原版设定，表现优于 ACT-EEF。

CFG-DP:
  已实现 prev_action condition / CFG / EAG / executor_future 对齐等核心原型，
  平滑性可以改善，但当前多组 ckpt 都出现任务性能下降和固定动作模式。
```

### 阶段性结论

我目前不再把 CFG 当成一定优于原版 DP 的路线，而是把它作为一个需要严谨 ablation 的改进分支。  
当前主线仍以原版 `DP-EEF` 作为 baseline，然后用 `DP-Joint`、`ACT-Joint`、`CFG-DP` 分别做对比。

---

## 2. 我排查了 CFG 模型只会固定动作的问题

### 问题

多个 CFG ckpt 在不同 `prev_cond_steps` 和 `dropout` 下都出现类似现象：

```text
1. 胶棒换位置后，机械臂仍倾向于同一个夹取位置
2. 夹爪动作模式高度相似
3. CFG_W 调整后平滑性变化明显，但任务成功率下降
4. 某些版本会在不同 obs 下输出接近固定的抓取轨迹
```

这说明问题不能只归因于部署层，训练/数据/视觉条件都有排查价值。

### 我检查的内容

我按下面几个维度对 CFG 和原版 DP 做了对比：

```text
1. ckpt 里的 shape_meta 是否正确
2. dataset 的 camera key 顺序是否正确
3. 部署时 CFG_VIDEO_DEVICES 是否和训练 camera_0/1/2 对应
4. normalizer 是否正常加载
5. obs_encoder 是否参与训练
6. fixed prev_action + varying obs 时输出是否变化
7. fixed obs + varying prev_action 时输出是否变化
```

### 检查结果

CFG ckpt 显示：

```text
workspace: arx5_dp_cfg.train_diffusion_unet_hybrid_workspace_cfg.TrainDiffusionUnetHybridWorkspace
policy: arx5_dp_cfg.diffusion_unet_hybrid_image_policy_cfg.DiffusionUnetHybridImagePolicy
dataset_path: data_local/glue_motion
prev_cond_steps: 3
prev_chunk_dropout: 0.5
prev_action_mode: future
shape_meta_obs_order: ['camera_0', 'camera_1', 'camera_2',
                       'robot0_eef_pos', 'robot0_eef_rot_axis_angle',
                       'robot0_gripper_width']
dataset_rgb_keys: ['camera_0', 'camera_1', 'camera_2']
obs_encoder_trainable_params: 33591264
```

这说明：

```text
1. 视觉 encoder 是参与训练的
2. camera key 的命名没有明显错位
3. normalizer 和 shape_meta 能加载
4. CFG 分支确实进入了模型
```

但是从行为上看，CFG 仍可能过度依赖 `prev_action` 或者学到数据里的主导轨迹模式，导致视觉条件对最终动作的调制不够强。

### 当前结论

CFG 当前最主要的问题不是“完全没有接入视觉”，而更像是：

```text
1. prev_action condition 太强，压过了 obs 的影响
2. 数据中某类接近/夹取模式占比过高
3. 从零训练 CFG 版本时，策略退化到了平均动作或主导动作模式
4. 在线部署的 condition 分布和训练里的 future prev_action 分布仍有差异
```

后续如果继续 CFG，更合理的路线是从表现最好的原版 DP 初始化，再微调 CFG 分支，而不是完全从零训练。

---

## 3. 我确认了 CFG_W=0 不能等价于原版 DP

### 问题

我尝试把 `CFG_W=0` 当成“关闭 CFG”，但实际表现仍明显差于原版 DP。

### 原因

`CFG_W=0` 使用的是 CFG 网络里的 unconditional 分支：

```text
epsilon_guided = epsilon_uncond + w * (epsilon_cond - epsilon_uncond)
```

当：

```text
w = 0
```

实际得到的是：

```text
epsilon_guided = epsilon_uncond
```

这不是原版 DP，因为 CFG 网络多了 prev_action encoder、mask、dropout、condition 融合结构。  
所以：

```text
原版 DP baseline != CFG-DP with CFG_W=0
```

### 当前做法

后续对比必须分开：

```text
原版 DP:
  scripts/run_dp_pro.sh

CFG-DP:
  scripts/run_dp_cfg_pro.sh
```

不能用 `CFG_W=0` 替代原版 DP。

---

## 4. 我重新处理了 glue_mini 数据，并生成了 joint 可用版本

### 问题

采集 `glue_mini` 后，我需要同时训练：

```text
1. DP-EEF
2. DP-Joint
3. 后续 CFG-DP
```

原始 `glue_mini` 的 `action` 是 EEF 格式：

```text
action = [x, y, z, rx, ry, rz, gripper]
```

DP-Joint 需要：

```text
action = [q1, q2, q3, q4, q5, q6, gripper]
```

### 数据清洗

我按安全流程删除了坏 episode：

```text
1. 先复制出临时数据包
2. 在临时数据包里删除指定 episode
3. 检查 episode 数、step 数、video 编号
4. 确认无误后再替换原数据包
```

这避免了直接在原数据上误删导致无法恢复。

### Joint 数据生成

我新增了脚本：

```text
diffusion_policy-main/arx5_collector/scripts/make_joint_dataset.py
```

它会复制原始 dataset，并把：

```text
replay_buffer.zarr/data/action
```

改写为：

```text
[robot_joint[:6], robot_gripper[:1]]
```

生成的数据包：

```text
data_local/glue_mini_joint
```

校验结果：

```text
glue_mini
  episodes 62 steps 14397
  max_action_joint_err 2.1712868
  max_action_eef_err 0.0558541

glue_mini_joint
  episodes 62 steps 14397
  max_action_joint_err 0.0
  max_action_eef_err 2.1717460
```

这说明：

```text
1. 原始 glue_mini 没被改坏，仍是 EEF action
2. glue_mini_joint 已经严格变成 joint action
3. 两个数据包可以分别用于 EEF 和 Joint 对比实验
```

---

## 5. 我制定了 EEF -> Joint 连续训练方案

### 目标

先用同一组数据训练一版 EEF，再紧接着训练一版 Joint，第二天直接测试两者差别。

### 启动方式

当前推荐命令：

```bash
cd /media/star/Elyos_PSSD/ARX5/CY_arx5_dp
mkdir -p logs/glue_mini_eef_then_joint

nohup bash -lc '
RUN_NAME=glue_mini_dp_eef_200 \
DATASET_PATH=data_local/glue_mini \
EPOCHS=200 \
BATCH_SIZE=16 \
NUM_WORKERS=4 \
VAL_WORKERS=2 \
VAL_EVERY=25 \
MAX_VAL_STEPS=50 \
CHECKPOINT_EVERY=25 \
WANDB_MODE_ARG=offline \
./scripts/train_dp_eef.sh

RUN_NAME=glue_mini_dp_joint_200 \
DATASET_PATH=data_local/glue_mini_joint \
EPOCHS=200 \
BATCH_SIZE=16 \
NUM_WORKERS=4 \
VAL_WORKERS=2 \
VAL_EVERY=25 \
MAX_VAL_STEPS=50 \
CHECKPOINT_EVERY=25 \
TARGET_FREQUENCY=20 \
WANDB_MODE_ARG=offline \
./scripts/train_dp_joint.sh
' > logs/glue_mini_eef_then_joint/nohup.log 2>&1 &
```

### 输出位置

```text
EEF:
diffusion_policy-main/data/outputs/manual/glue_mini_dp_eef_200/checkpoints/latest.ckpt

Joint:
diffusion_policy-main/data/outputs/manual/glue_mini_dp_joint_200/checkpoints/latest.ckpt
```

### best epoch 选择原则

我不只看 `train_loss`，而是按下面顺序挑：

```text
1. 先看 val_loss 是否在某个阶段开始上升
2. 选 100 / 150 / latest 这几个 ckpt 上机测试
3. 用真实成功率决定 best epoch
4. 如果 train_loss 很低但实际只会固定动作，视为过拟合或策略退化
```

当前 validation 设置：

```text
VAL_EVERY=25
MAX_VAL_STEPS=50
CHECKPOINT_EVERY=25
```

这个配置用于小数据真实机器人训练比较合适：不会太拖训练，又能看到过拟合趋势。

---

## 6. 我处理了 SpaceMouse 连接问题

### 问题

采集时出现：

```text
spnav.SpnavConnectionException: failed to connect to the space navigator daemon
```

同时 `evtest` 显示：

```text
This device is grabbed by another process.
```

`fuser` 显示有多个 `spacenavd` 占用同一个设备：

```text
root 1123 spacenavd
root 1835283 spacenavd
```

### 原因

SpaceMouse 设备存在被旧的 `spacenavd` 进程占用的情况。  
这会导致：

```text
1. evtest 读不到事件
2. Python spnav 连接失败
3. 采集脚本里 motion 一直是 0
```

### 处理思路

后续遇到同类问题时，先检查：

```bash
ls /dev/input/by-id | grep -i 3dconnexion
sudo fuser -v /dev/input/by-id/usb-3Dconnexion_SpaceMouse_Wireless_BT-event-joystick
systemctl status spacenavd --no-pager
```

如果有多个 `spacenavd` 或旧进程占用，需要先清理占用，再重新启动 daemon。  
这个问题属于外设服务状态问题，不是采集代码本身的问题。

---

## 7. 我处理了 USB 相机和 RealSense 重插后的设备编号问题

### 问题

拔插 USB 相机后出现：

```text
RuntimeError: Failed to open /dev/video0
```

随后扫描发现可打开的设备变成：

```text
/dev/video1
/dev/video4
/dev/video6
/dev/video10
/dev/video12
```

并且重新插拔 USB 相机后，RealSense 设备可能临时消失。

### 原因

Linux 下 `/dev/video*` 编号不是固定的。  
拔插相机后，USB 相机和 RealSense 的 video node 会重新分配，所以原来写死的：

```text
video0, video6, video12
```

可能失效。

### 当前处理方式

先用 OpenCV 扫描当前能打开的设备：

```bash
python - <<'PY'
import cv2
for i in range(0, 20):
    cap = cv2.VideoCapture(i, cv2.CAP_V4L2)
    ok = cap.isOpened()
    if ok:
        ret, frame = cap.read()
        print(f"/dev/video{i}: open={ok}, read={ret}, shape={None if frame is None else frame.shape}")
    cap.release()
PY
```

然后再把启动脚本里的设备编号改成当前实际编号。  
后续更稳定的方案是按相机 serial 或 `/dev/v4l/by-id` 做绑定，减少手动改编号。

---

## 8. 我重新分析了“停在胶棒前面夹空气”的问题

### 问题

当前多个版本都出现过：

```text
1. 机械臂能朝胶棒方向靠近
2. 但会停在胶棒前面一小段距离
3. 然后提前闭合夹爪
4. 有时需要二次修正才能真正夹到
```

### 当前判断

这个现象更像数据策略问题，而不是 PID 或时间对齐问题。

如果部署日志中出现：

```text
pose 还没真正到胶棒位置
gripper_minmax 已经明显下降
```

说明模型学到的是“胶棒在夹爪前方时就开始闭合”，也就是闭合时机偏早。

### 对采集策略的修正

我不应该在胶棒前面刻意长时间停顿，因为模型可能学到：

```text
到胶棒前以后应该停住
```

更合理的采集节奏是：

```text
1. 快速靠近胶棒附近
2. 最后 2~3 cm 放慢
3. 让胶棒真正进入夹爪两指之间
4. 再闭合夹爪
5. 闭合后保持 0.2~0.5 s
6. 再抬起
```

重点不是“停下来识别胶棒”，而是让数据里有足够多帧表达：

```text
夹爪中心对准胶棒 -> 胶棒进入夹爪内部 -> 闭合 -> 保持 -> 抬起
```

### 后续建议

后续可以补一小组专门针对最后 2 cm 对准和闭合的数据：

```text
1. 胶棒偏左：最后对准再夹
2. 胶棒偏右：最后对准再夹
3. 胶棒偏远：最后多往前补一点再夹
4. 胶棒偏近：不要过冲，包住后夹
```

先采 20~30 组小数据验证，不急着重采大规模数据。

---

## 9. 当前下一步计划

### 明天优先测试

```text
1. glue_mini_dp_eef_200
2. glue_mini_dp_joint_200
```

测试时不要只测 `latest.ckpt`，优先选：

```text
epoch 100
epoch 150
latest
```

每个 ckpt 用相同的胶棒位置测试：

```text
1. 中间近
2. 中间远
3. 左侧
4. 右侧
5. 边缘
```

### 暂缓项

```text
1. CFG 继续大规模训练
2. 工程补偿夹取距离
3. 继续改 PID
```

原因是当前主要问题更像模型学到的闭合时机和数据分布问题。  
先把 EEF / Joint 在同一组干净数据上的表现测清楚，再决定是否继续采数据或微调 CFG。
